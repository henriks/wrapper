# wra-xcvq: Option 1 Tokio-boundary architecture refactor

Work the `tk` epic `wra-xcvq` toward completion over at most 40 iterations.

## Epic Goal
Implement or explicitly supersede refactoring option 1: make agentvm a Tokio application at orchestration and byte-stream I/O boundaries, while keeping single-owner protocol/state cores synchronous and composed-fs/vhost filesystem request execution bounded blocking.

## Ground Rules
- Use `tk` for task state. Start/close child tickets as work begins/completes and add notes for significant implementation findings.
- Respect implementation dependencies from tickets; begin with unblocked foundational work.
- Prefer deletion, consolidation, and clearer module boundaries over layering more code.
- Config-file compatibility is the only compatibility requirement. If config parsing/serialization/defaults/field semantics change, update `vm-frontend/config-json.md` and tests in the same change.
- For arbitrary input/output changes, add or update fuzz coverage.
- If validation says the appliance must be rebuilt, tell the user exactly what needs rebuilding and why, then switch to other useful epic work until the user confirms rebuild completion.
- Do not mark tickets or the epic complete unless changes have passed live-capable validation.

## Initial Dependency-Aware Order
1. `wra-9glk` establish root workspace/shared crate boundaries.
2. `wra-09v9` split `vm-frontend/src/main.rs` into responsibility modules.
3. `wra-r070` typed domain errors, then `wra-rjt4` tracing.
4. `wra-n0fe` Tokio application edge/supervisor skeleton.
5. Protocol/shared work: `wra-cvmy`, then `wra-jkeg`, `wra-662v`, `wra-yl7i` backend boundaries as applicable.
6. Launch/runtime async edge work: `wra-zqci`, `wra-73tn`, `wra-f762`, `wra-gq8e`.
7. Cleanup/refinement: `wra-1esa`, `wra-6n71`, `wra-0a0r`, and any bugs discovered.

## Key Files From Epic
- `vm-frontend/src/main.rs`
- `vm-frontend/src/launch.rs`
- `vm-frontend/src/vmnet_runtime.rs`
- `vm-frontend/src/vmnet_poller.rs`
- `vm-frontend/src/vmnet_service_io.rs`
- `vm-frontend/src/payload_client.rs`
- `vm-frontend/src/docker_proxy.rs`
- `guest-service/src/lib.rs`
- `composed-fs/src/lib.rs`

## Checklist
- [x] Inspect current ticket graph and select the next unblocked child ticket (`wra-9glk`).
- [x] Start selected ticket with `tk start <id>` and add planning note.
- [x] Implement the selected ticket with minimal, idiomatic changes (root workspace slice for `wra-9glk`).
- [x] Update tests, fuzz targets, and docs relevant to the change (kept existing validation docs/commands unchanged; added standalone `[workspace]` to `vm-frontend/fuzz/Cargo.toml` so fuzz remains independently invocable).
- [x] Run focused tests while iterating.
- [x] Run `./vm-frontend/validate.sh required` before considering code complete.
- [x] If appliance rebuild is required, notify user and continue non-blocked work until confirmation (not needed for `wra-9glk`; required validation passed without rebuild request).
- [x] Close completed child tickets only after required validation passes in live-capable environment (`wra-9glk` closed).
- [ ] Continue through dependent tickets until `wra-xcvq` is implemented or explicitly superseded.

## Completed Ticket: `wra-09v9` split vm-frontend main
- [x] Select next unblocked ticket after `wra-9glk`.
- [x] Start `wra-09v9` and add planning note.
- [x] Map `main.rs` responsibilities and choose the first safe extraction slice.
- [x] Extract focused modules without behavior or config semantic changes.
  - [x] Extract appliance artifact freshness gate into `vm-frontend/src/appliance.rs`.
  - [x] Extract payload-client CLI parsing and sync-flush helper into `vm-frontend/src/payload_cli.rs`.
  - [x] Extract vmnet-gateway CLI parsing into `vm-frontend/src/vmnet_cli.rs`.
  - [x] Extract `.sandbox/config.json`/setup-tool schema and file helpers into `vm-frontend/src/config.rs`.
  - [x] Extract wrapper MITM CA bootstrap into `vm-frontend/src/tls_bootstrap.rs`.
  - [x] Extract live self-test implementation into `vm-frontend/src/self_test.rs`.
  - [x] Extract wrapper command parsing/assembly into `vm-frontend/src/wrapper.rs`.
  - [x] Extract frontend launch/prepare CLI and runtime assembly into `vm-frontend/src/launch_cli.rs`.
  - [x] Decide remaining production `main.rs` is acceptable as a thin entry/dispatch layer (tests still live below it).
- [x] Run focused tests and broader vm-frontend offline validation.
- [x] Run required validation before closing `wra-09v9`.
- [x] Close `wra-09v9` after live-capable required validation passed.

## Completed Ticket: `wra-r070` typed domain errors
- [x] Select next unblocked ticket after `wra-09v9`.
- [x] Start `wra-r070` and add planning note.
- [x] Inventory current `Result<_, String>` / string-formatting error boundaries across option-1 files.
- [x] Convert first narrow module seam (`vm-frontend/src/config.rs`) to typed `ConfigError`/`ConfigResult` without changing config semantics.
- [x] Add/update focused tests to assert structured config failures where practical.
- [x] Run focused config validation.
- [x] Continue replacing stringly errors in CLI/launch/payload/vmnet modules.
  - [x] Convert `vm-frontend/src/payload_cli.rs` to typed `PayloadCliError`/`PayloadCliResult`.
  - [x] Convert `vm-frontend/src/vmnet_cli.rs` to typed `VmnetCliError`/`VmnetCliResult`.
  - [x] Introduce a top-level `CliError`/`CliResult` boundary for binary command dispatch.
  - [x] Convert appliance freshness errors to typed `ApplianceError`/`ApplianceResult`.
  - [x] Convert wrapper MITM CA bootstrap errors to typed `TlsBootstrapError`/`TlsBootstrapResult`.
  - [x] Introduce `WrapperError`/`WrapperResult` for wrapper dispatch and configured launch defaults.
  - [x] Introduce `LaunchCliError`/`LaunchCliResult` for launch dispatch.
  - [x] Convert deeper launch helpers (`frontend_config_from_args`, `runtime_mounts`, `guest_payload_env`, guest share parsers) to `LaunchCliResult`.
  - [x] Convert project lock/reset, local HTTP smoke upstream setup, and no-net validation to typed `LaunchCliError` variants.
  - [x] Further reduce temporary `LaunchCliError::Message(String)` catch-all or decide remaining shared-helper bridge is acceptable.
- [x] Run broader vm-frontend offline validation after current typed-error slices.
- [x] Run required validation before closing `wra-r070`.
- [x] Add final validation note and close `wra-r070` after live-capable required validation passed.

## Completed Ticket: `wra-rjt4` tracing for launch/runtime supervision
- [x] Select next unblocked ticket after `wra-r070`.
- [x] Start `wra-rjt4` and add planning note.
- [x] Add initial structured tracing dependency/infrastructure without changing intentional CLI phase output.
- [x] Instrument first launch/runtime supervision seam.
  - [x] Add launch spans/events for frontend startup, service readiness, QEMU spawn/PID/wait/exit/timeout, termination, and unfinished-drop cleanup.
  - [x] Replace diagnostic composed-fs/config-fs/vmnet service-thread `eprintln!` failures with structured `tracing::error!` events.
- [x] Run focused validation for initial tracing slice.
- [x] Continue tracing through command dispatch, launch CLI payload lifecycle, docker proxy, vmnet runtime policy/proxy events, and shutdown/failure paths.
  - [x] Add command-dispatch tracing events in `main.rs`.
  - [x] Add launch CLI lifecycle events for configured launch spans, local smoke upstream, payload listener readiness, payload execution mode/exit code, and qemu success/timeout/failure.
  - [x] Add docker unix proxy service/client/failure tracing.
  - [x] Add vmnet runtime policy/proxy edge tracing or decide existing event-log coverage plus boundary traces is sufficient.
  - [x] Decide whether a binary tracing subscriber/configuration is needed for `wra-rjt4` acceptance or whether library events should remain subscriber-agnostic for now.
- [x] Run focused validation after vmnet/subscriber tracing slice.
- [x] Run broader vm-frontend offline validation before required validation.
- [x] Run required validation before closing `wra-rjt4`.
- [x] Add final validation note and close `wra-rjt4` after live-capable required validation passed.

## Completed Ticket: `wra-n0fe` Tokio application edge and supervisor skeleton
- [x] Select next unblocked ticket after `wra-rjt4`.
- [x] Start `wra-n0fe` and add planning note.
- [x] Add Tokio dependency/features for the application edge and update lockfile for offline validation.
- [x] Introduce async binary entry boundary without changing existing synchronous command behavior.
- [x] Add initial `LaunchSupervisor` skeleton with typed task specs/results and shutdown/cancellation API.
  - [x] Preserve composed-fs/config-fs as explicitly blocking service kinds rather than Tokio core-worker tasks.
  - [x] Add tests for task classification, shutdown observation, and no-spawn planned results.
- [x] Run focused validation for initial Tokio/supervisor slice.
- [x] Continue from skeleton toward supervised task ownership/readiness/failure APIs without converting vmnet internals yet.
  - [x] Add per-task watch-backed status state and task status subscriptions.
  - [x] Add `SupervisorTaskController` handles for explicit starting/ready/finished/failed/cancelled reporting.
  - [x] Add tests for readiness/failure publication and one controller per planned task.
- [x] Decide whether this skeleton is sufficient for `wra-n0fe` or add a small no-spawn supervisor run plan API before validation.
  - [x] Add no-spawn `run_until_shutdown` API that preserves terminal task status and cancels unfinished planned tasks with the shutdown reason.
  - [x] Decide skeleton surface is sufficient for `wra-n0fe` pending required validation: async binary edge exists, supervisor APIs expose typed task ownership/readiness/failure/cancellation, and no long-lived services are newly detached or converted internally.
- [x] Run broader vm-frontend offline validation before required validation.
- [x] Run required validation before closing `wra-n0fe`.
- [x] Add final validation note and close `wra-n0fe` after required validation passed.

## Completed Ticket: `wra-cvmy` shared payload protocol crate and codecs
- [x] Inspect ready/dependency state after closing `wra-n0fe` and select protocol/shared ticket `wra-cvmy`.
- [x] Start `wra-cvmy` and add implementation planning note.
- [x] Inventory duplicated payload frame protocol in `vm-frontend/src/payload_client.rs` and `guest-service/src/lib.rs`.
- [x] Extract first shared crate slice.
  - [x] Add workspace crate `payload-protocol` / `agentvm-payload-protocol`.
  - [x] Move frame constants, `FrameKind`, `Frame`, `FrameError`, sync decoder, header decode helper, encode helper, and fragmentation/oversize tests into the shared crate.
  - [x] Make `guest-service` re-export the shared protocol crate rather than owning a duplicate implementation.
  - [x] Change `vm-frontend/src/payload_client.rs` frame send/receive helpers to use shared `encode_frame`, `decode_header`, `FrameKind`, and frame-size errors.
  - [x] Add `vm-frontend/fuzz/fuzz_targets/payload_protocol_frame.rs` for arbitrary frame bytes and bounded round-trip coverage.
- [x] Run focused validation for first shared protocol slice.
- [x] Continue extraction toward request/event structs and async codec APIs.
  - [x] Move `PayloadRequest`, `DiagnosticRequest`, `PayloadEvent`, exit/signal/resize frame schemas, event decoding, and signal/resize payload builders into `agentvm-payload-protocol`.
  - [x] Preserve existing `vm-frontend::payload_client` public API by re-exporting moved request/event types.
  - [x] Add Tokio-ready `read_frame_async`/`write_frame_async` APIs that keep oversized receive rejection before payload allocation and reject oversized public frames before async write encoding.
  - [x] Add protocol tests for request defaults, control payload schemas, event decoding, and async frame helpers.
- [x] Run focused validation after request/event/async protocol slice.
- [x] Update validation gate/docs so `payload-protocol` is covered by required formatting/offline tests and the new fuzz target is documented.
- [x] Run broader validation for `wra-cvmy` and decide the shared protocol surface is sufficient for ticket closure.
- [x] Run required live-capable validation and close `wra-cvmy`.

## Completed Ticket: `wra-jkeg` async payload client/session plumbing
- [x] Inspect ready/dependency state after closing `wra-cvmy` and select dependent payload ticket `wra-jkeg`.
- [x] Start `wra-jkeg` and add implementation planning note.
- [x] Add first additive async payload session slice without changing existing synchronous launch/TUI paths.
  - [x] Add `AsyncPayloadSession` on top of `agentvm-payload-protocol` async frame APIs.
  - [x] Use Tokio split read/write ownership rather than cloned blocking `TcpStream` for the new async session.
  - [x] Use bounded `mpsc` command/event channels for input, signal, resize, and payload events.
  - [x] Abort reader/writer tasks when the async session is dropped.
  - [x] Add async tests for request/control frame writes, output/exit events, and partial-frame IO errors.
- [x] Run focused validation for the first async payload session slice.
- [x] Add async ping/diagnostic timeout/cancellation helpers.
  - [x] Add `AsyncPayloadSession::cancel()` for explicit cancellation of reader/writer tasks.
  - [x] Add `ping_payload_async_io()` using shared async frame codecs.
  - [x] Add `run_diagnostic_async_io()` with `tokio::time::timeout`, async output streaming, exit/failure handling, and partial-frame deadline coverage.
  - [x] Add async tests for cancellation closing event/command paths, ping frame exchange, diagnostic output/exit, and diagnostic timeout on a partial frame.
- [x] Run focused validation after async ping/diagnostic/cancellation slice.
- [x] Decide and add minimal TUI bridge compatibility seam.
  - [x] Add cloneable `AsyncPayloadCommandSender` for async, nonblocking `try_send`, and sync `blocking_send` command submission.
  - [x] Expose `AsyncPayloadSession::command_sender()` so TUI/control frontends can feed the async writer without owning/cloning a raw stream.
  - [x] Add focused test for command-channel backpressure (`WouldBlock`) and closed-channel cancellation behavior.
- [x] Run broader vm-frontend offline validation for `wra-jkeg` current slice.
- [x] Decide whether `wra-jkeg` acceptance requires wiring async sessions into production launch/TUI now, or whether reusable async client plumbing is sufficient with production integration delegated to `wra-zqci`/`wra-yl7i`.
  - [x] Accepted `wra-jkeg` as reusable async payload client/session plumbing, per the ticket's scope-update note; production launch-supervisor integration remains delegated to `wra-zqci`, and full TUI/control-socket frontend behavior remains delegated to `wra-yl7i`.
- [x] Run required live-capable validation and close `wra-jkeg`.

## Current Ticket: `wra-zqci` async launch process/readiness supervision
- [x] Inspect ready/dependency state after closing `wra-jkeg` and select `wra-zqci` as the next dependency-order option-1 ticket.
- [x] Start `wra-zqci` and add implementation planning note.
- [x] Inspect current launch lifecycle ownership/readiness seams.
  - [x] Confirm `start_frontend_with_policy` still owns blocking state-disk creation, detached composed-fs/config-fs/vmnet service threads, socket-path readiness polling, optional docker proxy startup, and std QEMU spawn.
  - [x] Confirm `RunningFrontend` owns only the QEMU child plus a shared shutdown flag; service failures are logged but not propagated to QEMU/state handling yet.
  - [x] Confirm `LaunchSupervisor` currently provides task status/watch/controller primitives but is not wired into production launch.
- [x] Choose and implement the first additive async supervision slice without rewriting composed-fs/vmnet internals wholesale.
  - [x] Add Tokio process helpers in `launch.rs`: `run_qemu_process_async`, `spawn_qemu_process_async`, and `wait_for_qemu_async`.
  - [x] Preserve existing `QemuExit`/timeout semantics for the new async helper, including kill-on-timeout behavior via `tokio::time::timeout` and `tokio::process::Child`.
  - [x] Add async unit tests for nonzero process exit and timeout kill behavior.
- [x] Wire async process helpers into supervisor task status ownership.
  - [x] Add `run_supervised_qemu_process_async` that marks the QEMU task starting/ready/finished/failed around async spawn/wait/timeout.
  - [x] Keep `QemuExit` return semantics while publishing timeout as a failed supervisor task status.
  - [x] Add focused tests for supervised normal QEMU exit and timeout status publication.
- [x] Add async service readiness helper with supervisor status publication.
  - [x] Add `wait_for_path_async` using Tokio sleep/timeout boundaries while preserving the synchronous timeout message shape.
  - [x] Add `wait_for_service_ready_async` that marks service controllers ready or failed on readiness timeout.
  - [x] Add focused tests for async readiness success and timeout failure status publication.
- [x] Add state-writing supervised async QEMU runner surface.
  - [x] Add `run_supervised_qemu_process_with_state_async` that writes `state.json` as `running` after spawn and `exited`/`timed_out` after wait.
  - [x] Refactor shared supervised QEMU wait/status handling into `finish_supervised_qemu_wait`.
  - [x] Add focused tests for state updates on supervised async QEMU exit and timeout.
- [x] Add supervised blocking service readiness helper for bounded-blocking service startup.
  - [x] Add `spawn_supervised_blocking_service_until_ready`, which runs a blocking service through `tokio::task::spawn_blocking`, races readiness against early service exit, returns a join handle after readiness, and marks the service failed on timeout/early failure.
  - [x] Add focused tests for ready service handle return and early service failure status publication.
- [x] Add service handle wrapper for observing post-readiness blocking service failures.
  - [x] Change `spawn_supervised_blocking_service_until_ready` to return `SupervisedBlockingService` with retained controller and join handle.
  - [x] Add `SupervisedBlockingService::wait` to mark services finished on clean exit or failed on late service/join failure.
  - [x] Add focused test for late service failure after readiness.
- [x] Add first async QEMU-plus-service runner skeleton with service-failure ownership.
  - [x] Add `run_supervised_qemu_process_with_service_async`, which writes QEMU `running` state, races QEMU exit/timeout against a supervised service handle, kills QEMU on service failure, marks QEMU failed, writes `state.json` as `service_failed`, and returns the service error.
  - [x] Add focused test for service failure while QEMU is running.
- [x] Generalize async QEMU/service runner skeleton from one service to multiple supervised service handles.
  - [x] Add `run_supervised_qemu_process_with_services_async` and `wait_for_first_service_completion`.
  - [x] Make the one-service runner delegate to the multi-service helper.
  - [x] Add focused test with config-fs and vmnet handles where the first service failure stops QEMU and writes `service_failed` state.
- [x] Connect the multi-service runner to production launch preparation/service construction behind an additive async launch function.
  - [x] Add `run_frontend_until_qemu_exit_with_policy_and_timeout_async`, which performs launch prep/state, starts composed-fs/config-fs/vmnet through supervised bounded-blocking service helpers, waits for optional docker proxy readiness asynchronously, and runs QEMU through the multi-service supervised runner.
  - [x] Change multi-service monitoring to use `tokio::task::JoinSet` instead of detached monitor tasks.
  - [x] Leave production `start_frontend_with_policy` unchanged.
- [x] Decide whether and how to route production launch CLI through the async launch function, then run broader validation.
  - [x] Decision: do not route production launch CLI through the async function yet; keep the currently live-validated synchronous launch path until async shutdown/drop semantics for all services are explicit.
  - [x] Run broader vm-frontend offline validation for current `wra-zqci` async slices.
- [x] Tighten async launch lifecycle shutdown/status semantics before any production switch.
  - [x] Mark unfinished supervised services cancelled when QEMU exits normally or times out.
  - [x] Mark peer services cancelled when the first service failure terminates QEMU.
  - [x] Add focused tests for QEMU-exit service cancellation and peer-service cancellation on first service failure.
- [x] Add missing async lifecycle test for QEMU timeout cancelling unfinished services.
  - [x] Verify unfinished service statuses become `Cancelled { reason: "qemu timed out" }` when the supervised async QEMU runner times out.
- [x] Decide whether async lifecycle semantics are sufficient for production non-TUI routing.
  - [x] Route direct no-payload/plain `agentvm launch` through a new async-aware `run_async`/`run_launch_async` path.
  - [x] Keep TUI/payload launches and wrapper launches on the existing synchronous path for now to avoid changing payload/TUI lifecycle in `wra-zqci`.
  - [x] Extract shared QEMU exit formatting into `handle_qemu_exit`.
- [x] Run broader validation for production async launch routing.
- [x] Run required validation and close `wra-zqci`.
  - [x] `./vm-frontend/validate.sh required` passed; no appliance rebuild requested.
- [x] Start next dependency-order ticket `wra-662v` for the opt-in Rust/Tokio guest payload service.
  - [x] Reconfirmed scope: opt-in Rust service only; do not switch default away from Python in this ticket.
  - [x] Inspected current `guest-service` skeleton and Python `docker/guest-payload-server.py` baseline behavior.
- [x] Plan and implement the smallest tested `wra-662v` slice.
  - [x] Chose protocol/session-admission core before process execution.
  - [x] Added `GuestServiceLimits`/`GuestServiceState` with Python-baseline defaults for max clients, max diagnostics, initial timeout, and session I/O timeout.
  - [x] Added bounded client, single-primary, and diagnostic admission guards.
  - [x] Added async initial-frame handling over `AsyncRead`/`AsyncWrite`: ping OK, protocol/oversized failures attempt failure frames, unexpected initial frames fail, and valid primary/diagnostic requests are parsed/admitted but return explicit not-implemented failures.
  - [x] Added focused Tokio duplex/admission tests.
- [x] Add real diagnostic command execution with timeout/output caps before primary PTY execution.
  - [x] Diagnostic requests spawn `/bin/sh -c` with stdin closed and stdout/stderr piped.
  - [x] Stream bounded `OUTPUT` frames and send diagnostic `EXIT` JSON compatible with the frontend/Python schema.
  - [x] Cap diagnostic timeout to 60s and output to 4MiB; kill on timeout/truncation; report exit 124 for timeout and 125 for truncation.
  - [x] Added focused tests for stdout/stderr plus exit code, output-limit truncation, and timeout.
- [x] Add TCP listener/CLI serving path while keeping default Python path unchanged.
  - [x] Added `serve_tcp`/`serve_listener` around `tokio::net::TcpListener` with per-client task spawning.
  - [x] Added TCP bind/accept typed guest-service errors.
  - [x] Replaced the skeleton `agentvm-guest-service` binary with a Tokio CLI that accepts Python-compatible serving flags (`--tcp-port`, `--tcp-host`, `--max-clients`, `--max-diagnostics`, `--initial-timeout`, `--io-timeout`).
  - [x] Added CLI parser tests and a loopback listener ping test.
- [x] Decide next `wra-662v` direction: add opt-in appliance wiring before primary payload PTY/control handling.
  - Rationale: the Rust service is reachable and diagnostic-capable, so real opt-in plumbing gives future parity/live validation a concrete appliance path while Python remains default.
- [x] Add opt-in appliance wiring for the reachable Rust guest-service.
  - [x] `docker/guest-init.sh` now selects `python` by default and `rust` only when `agentvm_payload_service=rust` is present; `rust` requires executable `/usr/local/libexec/agentvm-guest-service` and fails closed if absent.
  - [x] `docker/build-appliance.sh` now supports `AGENTVM_PAYLOAD_SERVICE=rust`, requires `AGENTVM_GUEST_SERVICE_BIN`, and adds `agentvm_payload_service=rust` to the manifest kernel cmdline only for that opt-in.
  - [x] Added source-only shell tests for guest-init service selection and build-appliance opt-in/kernel-cmdline behavior.
- [x] Start primary payload execution parity instead of stopping before PTY work.
  - [x] Added an initial non-PTY Tokio primary runner that spawns `/bin/sh -c` with piped stdin/stdout/stderr.
  - [x] Streams stdout/stderr as `OUTPUT` frames through a bounded Tokio channel and sends normal `EXIT` JSON.
  - [x] Forwards `INPUT` frames into child stdin, parses `SIGNAL`/`RESIZE` frames, delivers signals to the child PID, and terminates on client disconnect/output write failure.
  - [x] Added focused tests for primary stdout/stderr plus exit code and stdin forwarding.
  - Caveat: full Python parity still requires PTY, controlling-terminal/session/process-group behavior, UID/GID/HOME setup, real resize handling, and slow-writer cleanup.
- [x] Continue primary parity with process-group/session and disconnect cleanup.
  - [x] Primary and diagnostic child commands now run in a new session via `setsid()` pre-exec.
  - [x] Primary signal forwarding and disconnect/write-failure cleanup now target the child process group rather than only the shell PID.
  - [x] Diagnostic timeout/truncation now SIGKILLs the child process group before the child fallback kill.
  - [x] The primary control loop disables further control reads after EOF/error, avoiding repeated EOF spin while waiting for child termination.
  - [x] Added focused tests for process-group signal forwarding and client-disconnect cleanup of a long-running primary payload.
  - Remaining parity gaps: PTY allocation/controlling terminal, real resize ioctl handling, UID/GID/HOME setup, graceful TERM-then-KILL escalation for primary processes that ignore TERM, and slow-writer deadlines/backpressure.
- [x] Run broader validation and required-validation attempt for `wra-662v` state.
  - [x] Broad offline validation passed: `sh docker/tests/test_guest_init.sh`; `bash docker/tests/test_build_appliance.sh`; `cargo test --workspace --offline`; `cargo fmt --all -- --check`.
  - [x] Required validation attempt #1 found a flaky `supervised_blocking_service_marks_failed_after_readiness` test race.
  - [x] Documented the flake as bug `wra-kolb` and fixed the test by keeping the fake late-failing service alive briefly after readiness.
  - [x] Focused validation for `wra-kolb` passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service_marks_failed_after_readiness -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`.
  - [x] Required validation attempt #2 timed out during vm-frontend offline tests after reporting `payload_session_runner_cancel_token_interrupts_blocked_receive` running for over 60 seconds; focused rerun passed immediately.
  - [x] Documented the required-validation timeout/hang as bug `wra-cqjc`.
  - Required validation did not complete and did not reach any appliance rebuild request; do not close `wra-662v`.
- [x] Continue iteration set 2 / iteration 1: resolve/triage `wra-cqjc` enough for another required-validation attempt.
  - [x] Hardened `PayloadCancelToken::register_stream` so a stream registered after cancellation is immediately shut down.
  - [x] Hardened `payload_session_runner_cancel_token_interrupts_blocked_receive` with bounded `recv_timeout`, server read timeout, and explicit close assertion.
  - [x] Focused validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_session_runner_cancel_token_interrupts_blocked_receive -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_session_runner -- --nocapture`.
  - [x] Broad validation passed: `cargo test --workspace --offline`; `cargo fmt --all -- --check`.
  - [x] Closed `wra-kolb` after focused and broad validation for the supervised blocking service flake fix.
- [x] Continue iteration set 2 / iteration 2: reran `./vm-frontend/validate.sh required` after `wra-cqjc` hardening.
  - [x] Validation progressed past the previous `payload_session_runner_cancel_token_interrupts_blocked_receive` hang and into live-smoke.
  - [x] Validation stopped at appliance freshness: `docker/build-appliance.sh` changed since `docker/out/artifact-manifest.json` was written (expected sha256 `3fe137f74ba1650bb130b54def3f7a3af055d30d17a7ee2e561b5813aad436bb`, current `55d90dca2ad53114b69dc6797a4be4378ca0b187c513906e158b44616481d2a1`).
  - [x] Closed `wra-cqjc`; the remaining required-validation blocker is stale appliance artifacts, not the payload cancellation hang.
  - [ ] `wra-662v` remains open/blocked until the appliance is rebuilt and required validation passes.
- [x] Continue iteration set 2 / iteration 3: while waiting for appliance rebuild, started independent option-1 ticket `wra-gq8e`.
  - [x] Added async Docker Unix proxy side-by-side with existing sync implementation: Tokio listener/upstream, `copy_bidirectional`, watch shutdown, bounded connection semaphore, and owned client tasks.
  - [x] Added focused async Docker proxy tests for bridge behavior, cancellation, and invalid limits.
  - [x] Focused validation passed: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline docker_proxy -- --nocapture`.
- [x] Continue iteration set 2 / iteration 4: wired async Docker proxy into async launch supervision/cancellation.
  - [x] Added optional `DockerProxy` managed task metadata and supervisor task kind.
  - [x] Async launch starts the Docker proxy through `spawn_supervised_async_service_until_ready` and the Tokio proxy implementation when the policy exposes a Docker API listener.
  - [x] Supervised service cancellation now can signal async services through a watch channel on QEMU exit/timeout or peer failure, while composed-fs/config-fs/vmnet remain bounded-blocking services.
  - [x] Added regression coverage that QEMU exit signals async service shutdown.
  - [x] Validation passed: focused Docker/supervisor tests plus full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 5 reflection and `wra-gq8e` cleanup.
  - [x] Reflection: current blocker is appliance freshness, not code instability; continue offline-testable option-1 slices but avoid duplicating sync/async paths.
  - [x] Removed the legacy sync Docker proxy per-client/threaded copy path. `start_docker_unix_proxy` now binds synchronously and runs the shared async proxy on a current-thread Tokio runtime in its service thread; all client bridging uses Tokio `copy_bidirectional`.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline docker_proxy -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervisor -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 6: documented `wra-gq8e` as validation-blocked and started `wra-73tn`.
  - [x] `wra-gq8e`: noted that the remaining sync service-thread wrapper is for legacy non-async launch paths but now reuses the shared Tokio proxy with no detached per-client copy threads.
  - [x] `wra-73tn`: introduced `VmnetCore` as a synchronous single-owner wrapper around `VmnetGateway`.
  - [x] `run_qemu_stream_until_eof` and `run_qemu_stream_tick` now use `VmnetCore`, with a narrow `gateway_mut` escape hatch for still-unextracted proxy/driver helpers.
  - [x] Added core-only test coverage without sockets/poller/QEMU I/O.
  - [x] Validation passed: focused vmnet core/runtime tests and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 7: continued `wra-73tn` core/driver seam extraction.
  - [x] DNS service/deferred-DNS owner-side helpers now take `VmnetCore` rather than raw `VmnetGateway`.
  - [x] `serve_vmnet_gateway` now uses `VmnetCore` for poll delays, timer TCP polling, QEMU guest-frame handling, and DNS worker completion/fail-closed paths.
  - [x] Remaining proxy/host-ingress access goes through explicit `core.gateway_mut()` escape hatches for later shrinking.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_runtime -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 8: reduced the `gateway_mut` escape hatch around proxy/TCP-connect paths.
  - [x] `pump_proxy_once` and `pump_proxy_ready` now take `VmnetCore`.
  - [x] TCP connect service submission/completion/fail-closed helpers now take `VmnetCore`.
  - [x] Production loop no longer passes raw `VmnetGateway` into proxy pump or TCP connect worker paths.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_runtime -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 9 reflection and host-ingress seam.
  - [x] Reflection: progress is healthy but still live-validation blocked by stale appliance artifacts; continue only offline-testable, code-reducing boundary slices.
  - [x] `pump_host_ingress_once` and `pump_host_ingress_ready` now take `VmnetCore`.
  - [x] Centralized host ingress session opening in `open_host_ingress_session`, returning guest frames plus structured events.
  - [x] Production loop no longer calls `host_ingress.open_session` directly with raw gateway access.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_runtime -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 10: tightened/documented `VmnetCore` adapter escape hatch.
  - [x] Internal `VmnetCore` deferred-DNS/completion helpers and `gateway_mut` are private to `vmnet_runtime`, not crate-visible.
  - [x] `gateway_mut` is documented as a narrow synchronous driver-adapter escape hatch; new runtime code should prefer explicit `VmnetCore` methods.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_runtime -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [x] Continue iteration set 2 / iteration 11: paused `wra-73tn` feature expansion and ran broad offline validation.
  - [x] Assessed `VmnetCore` seam as a useful mechanical stopping point until required/live-capable validation is available.
  - [x] Broad validation passed: `cargo test --workspace --offline`; `cargo fmt --all -- --check`.
  - [x] Did not retry `./vm-frontend/validate.sh required` because appliance artifacts are known stale and need rebuild first.
- [x] Continue iteration set 2 / iteration 12: started `wra-6n71` and reran required validation after appliance rebuild.
  - [x] Moved self-test-specific unit coverage from the top-level `main.rs` test module into `vm-frontend/src/self_test.rs`.
  - [x] Moved default artifact-manifest path helper ownership from `self_test.rs` into `launch_cli.rs` as shared CLI infrastructure.
  - [x] Focused validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline self_test -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline parses_frontend_prepare_defaults_and_policy -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
  - [x] `./vm-frontend/validate.sh required` passed after the user rebuilt appliance artifacts.
  - [x] Closed `wra-73tn` after required/live validation exercised the production vmnet runtime with the `VmnetCore` seam.
  - [x] Kept `wra-gq8e` open pending Docker-proxy live coverage and `wra-662v` open pending Rust opt-in appliance live validation or explicit handoff to `wra-y335`.
- [x] Continue iteration set 2 / iteration 13 reflection and Docker live validation.
  - [x] Reflection: validation is now ticket-specific, not globally artifact-blocked; avoid over-claiming default-Python appliance validation for Rust opt-in work.
  - [x] `./vm-frontend/validate.sh live-docker` passed, covering container egress allow, no-net Docker egress denial, and host-to-container published-port access.
  - [x] Closed `wra-gq8e` after required validation plus Docker-specific live validation.
  - [x] Left `wra-662v` open because Rust opt-in appliance live validation has not been run from an `agentvm_payload_service=rust` manifest.
- [x] Continue iteration set 2 / iteration 14: continued `wra-6n71` and revalidated after appliance rebuild.
  - [x] Added `vm-frontend/src/self_test_payload.rs` for self-test guest payload script generation and hostile payload steps.
  - [x] Kept `self_test.rs` focused on self-test CLI/orchestration plus host-side SQLite helpers.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline self_test_payload -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline self_test::tests -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; `./vm-frontend/validate.sh required`.
  - [x] Built Rust guest-service binary offline, but confirmed the current rebuilt appliance manifest is still default Python and cannot be counted as Rust opt-in live validation.
- [x] Continue iteration set 2 / iteration 15: moved validation entrypoint off production `agentvm` and closed `wra-6n71`.
  - [x] `agentvm self-test` now returns `unknown command: self-test`.
  - [x] `validate.sh` now runs self-test scenarios through `--bin agentvm-frontend`.
  - [x] README and validation workflow examples were updated to match.
  - [x] Required validation passed: `./vm-frontend/validate.sh required`.
  - [x] Closed `wra-6n71` after validation.
- [x] Continue iteration set 2 / iteration 16: started `wra-1esa` and removed first compatibility-only CLI shim.
  - [x] Removed wrapper pre-scan/custom messages for `--docker`, `--docker-machine`, and `--pass-env`.
  - [x] Removed flags now fail as normal unknown clap arguments; config compatibility/migration is unchanged.
  - [x] Validation passed: `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_rejects_removed_flags_as_unknown_arguments -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`.
- [ ] Next: continue `wra-1esa` by removing another compatibility-only CLI alias/path, or validate/defer the Rust opt-in appliance path for `wra-662v`.

## Verification Log
- 2026-05-17: Read `tk show wra-xcvq`; initial child/dependency ordering captured from ticket output.
- 2026-05-17 iteration 1: Read `tk show wra-9glk`, `tk dep tree --full wra-9glk`, crate manifests, and `Makefile`; selected `wra-9glk` as the unblocked foundational ticket; ran `tk start wra-9glk` and added an implementation planning note.
- 2026-05-17 iteration 2: Added root `Cargo.toml`, root `Cargo.lock`, shared workspace metadata/dependencies, root `target/` ignore, and standalone fuzz workspace marker; removed stale per-member lockfiles while keeping `vm-frontend/fuzz/Cargo.lock`.
- 2026-05-17 iteration 2 validation: `cargo metadata --offline --format-version 1 --no-deps` from repo root passed and listed `agentvm-composed-fs`, `agentvm-frontend`, `agentvm-guest-service`, `virtiofsd`; `cargo check --manifest-path vm-frontend/fuzz/Cargo.toml --offline` passed; `cargo test --manifest-path guest-service/Cargo.toml --offline` passed; `cargo test --manifest-path composed-fs/Cargo.toml --offline` passed; `cargo test --manifest-path vm-frontend/Cargo.toml --offline` passed; rustfmt checks for composed-fs, guest-service, vm-frontend, and fuzz passed.
- 2026-05-17 iteration 3 validation: `./vm-frontend/validate.sh required` passed end-to-end, including docs/fmt/offline tests, guest-services, fuzz-check, `live-smoke`, and `live-setup-tools`; no appliance rebuild requested.
- 2026-05-17 iteration 3 ticket state: added final validation note to `wra-9glk` and closed it.
- 2026-05-17 iteration 4 ticket state: `tk ready` showed `wra-09v9`, `wra-cvmy`, and `wra-0a0r` ready; selected dependency-order ticket `wra-09v9`, ran `tk start wra-09v9`, and added an implementation planning note.
- 2026-05-17 iteration 4 inspection: `vm-frontend/src/main.rs` is 5,784 lines; initial top-level symbol map confirms it mixes dispatch, wrapper parsing, config schema/migration, setup-tool helpers, payload/vmnet/self-test parsers, runtime mounts/env, appliance freshness, local smoke helpers, and tests.
- 2026-05-17 iteration 5 extraction: created `vm-frontend/src/appliance.rs` for appliance artifact freshness manifest structs, required source input list, source-hash validation, and SHA-256 helper; `main.rs` now imports the freshness gate and retains test-only imports for existing freshness tests. `main.rs` decreased to 5,692 lines.
- 2026-05-17 iteration 5 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` passed; `cargo test --manifest-path vm-frontend/Cargo.toml --offline appliance_source_hashes -- --nocapture` passed; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_config_from_args -- --nocapture` passed.
- 2026-05-17 iteration 6 reflection: Completed one foundational workspace ticket and two behavior-preserving `main.rs` extractions. Small cohesive extraction slices with focused tests are working well and avoid config/CLI semantic drift. No appliance rebuild is currently needed. The main blocker is `main.rs` test coupling: existing tests still live in the large binary test module and sometimes require test-only imports from extracted modules. Continue extracting modules, then consider moving tests alongside modules when that reduces coupling without making the diff too broad.
- 2026-05-17 iteration 6 extraction: created `vm-frontend/src/payload_cli.rs` for payload-client CLI parsing, payload exit status mapping, and sync diagnostic filesystem flush helper. `main.rs` decreased to 5,539 lines.
- 2026-05-17 iteration 6 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline guest_sync -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline flush_guest_filesystems -- --nocapture` all passed.
- 2026-05-17 iteration 7 extraction: created `vm-frontend/src/vmnet_cli.rs` for vmnet-gateway CLI parsing and `VmnetRuntimeConfig` construction; `main.rs` now delegates that parser and decreased to 5,397 lines.
- 2026-05-17 iteration 7 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_gateway_runtime_config -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_no_net -- --nocapture` all passed.
- 2026-05-17 iteration 8 extraction: created `vm-frontend/src/config.rs` for `.sandbox/config.json`/setup-tool schema, validation, migration, config paths, read/write, and setup-tool mise config writing. `main.rs` now delegates config helpers and decreased to 4,968 lines. This was a mechanical move only; no config field semantics/defaults/migration/serialization changed, so `vm-frontend/config-json.md` did not require a semantic update.
- 2026-05-17 iteration 8 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline setup_tool -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline config_share_shadow -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline legacy_config -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline config_schema -- --nocapture` all passed.
- 2026-05-17 iteration 9 extraction: created `vm-frontend/src/tls_bootstrap.rs` for wrapper/project MITM CA generation and private-key writing. `main.rs` delegates `ensure_wrapper_mitm_ca` and decreased to 4,891 lines. Paths, certificate naming, key permissions, and CA env behavior are unchanged.
- 2026-05-17 iteration 9 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_mitm_ca -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_env_sets_guest_ca_bundle -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 iteration 10 extraction: created `vm-frontend/src/self_test.rs` for self-test config parsing, live self-test orchestration, self-test payload script construction, published-container check helper, and SQLite concurrency smoke helpers. `main.rs` delegates `run_self_test` and decreased to 4,049 lines. This keeps the production `self-test` command behavior unchanged; `wra-6n71` owns moving live self-tests out of production CLI.
- 2026-05-17 iteration 10 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline self_test_payload -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline parses_self_test -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline reset_sqlite_concurrency_db -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 iteration 11 reflection: The mechanical extraction strategy is working: behavior has remained stable under focused and broad offline tests, and the work is unlocking clearer CLI/config/self-test/payload/vmnet boundaries. The main friction is test coupling to binary-private helpers, so extracted modules expose some `pub(crate)` helpers to preserve existing tests rather than moving all tests at once. No appliance rebuild has been needed. `main.rs` is still not a truly thin entry point because frontend launch config, runtime mounts/env/auth, policy construction, local smoke upstream, reset, readiness wait, and binary tests remain.
- 2026-05-17 iteration 11 extraction: created `vm-frontend/src/wrapper.rs` for wrapper command parsing/assembly, wrapper UI mode selection, setup-tool launch argument assembly, configured launch defaults, command override handling, project mise wrapping, and wrapper run dispatch. `main.rs` decreased to 3,474 lines.
- 2026-05-17 iteration 11 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 12 extraction: created `vm-frontend/src/launch_cli.rs` for frontend launch/prepare CLI parsing, `run_launch`, project lock/reset helpers, runtime mount and guest env assembly, policy construction, local HTTP smoke upstream, payload readiness wait, and no-net validation. Production `main.rs` is now ~418 lines before tests and primarily owns top-level dispatch, shared tiny parse/quoting/auth helpers, and usage output.
- 2026-05-17 iteration 12 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 13 validation: `./vm-frontend/validate.sh required` passed end-to-end after the final `main.rs` split, including docs/fmt/offline tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 13 ticket state: added final validation note to `wra-09v9` and closed it.
- 2026-05-17 iteration 14 ticket state: `tk ready` showed `wra-r070` unblocked after `wra-09v9`; ran `tk start wra-r070` and added an implementation planning note.
- 2026-05-17 iteration 14 inspection: stringly `Result<_, String>` boundaries remain concentrated in vm-frontend binary modules (`main.rs`, `launch_cli.rs`, `wrapper.rs`, `self_test.rs`, `payload_cli.rs`, `vmnet_cli.rs`, `tls_bootstrap.rs`, `appliance.rs`, `config.rs`) plus some protocol parser helpers. Start with narrow module seams and keep formatting at command boundaries.
- 2026-05-17 iteration 14 typed-error slice: converted `vm-frontend/src/config.rs` to `ConfigError`/`ConfigResult`, preserving user-facing messages and config semantics while retaining a temporary `From<ConfigError> for String` bridge for callers still returning `String`. Updated the share-shadow validation test to assert structured `ConfigError` variants as well as message text.
- 2026-05-17 iteration 14 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline config_share_shadow_validation -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline config_ -- --nocapture` all passed.
- 2026-05-17 iteration 15 typed-error slice: converted `vm-frontend/src/payload_cli.rs` to `PayloadCliError`/`PayloadCliResult` for clap parse wrapping, numeric option validation, env parsing, missing script/positive bounds checks, guest sync diagnostic exit failures, and underlying `PayloadClientError`. Converted `vm-frontend/src/vmnet_cli.rs` to `VmnetCliError`/`VmnetCliResult` for clap parse wrapping, missing socket, and no-net policy validation. Temporary `From<...> for String` bridges remain for top-level callers until command errors migrate.
- 2026-05-17 iteration 15 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client_config_reports_structured_parse_errors -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_gateway_runtime_config_requires_socket -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline flush_guest_filesystems -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_gateway_runtime_config -- --nocapture` all passed.
- 2026-05-17 iteration 16 reflection: accomplishments so far are strong foundational progress: `wra-9glk` and `wra-09v9` are closed after required validation, and `wra-r070` now has typed module errors for config, payload CLI, and vmnet CLI. The seam-by-seam approach is working because focused tests catch behavior drift while keeping diffs reviewable. The main drag is temporary typed-error-to-`String` bridging: wrapper/launch/self-test still return `String`, so typed errors can still be stringified when crossing those boundaries. Adjust approach by introducing top-level command errors before tackling the largest launch/self-test modules, then remove bridges as callers migrate. Next priorities: reduce bridges around wrapper/launch, then convert appliance/TLS/self-test or launch seams, followed by required validation.
- 2026-05-17 iteration 16 typed-error slice: introduced `CliError`/`CliResult` in `vm-frontend/src/main.rs` so binary command dispatch preserves `PayloadCliError`, `VmnetCliError`, `PayloadClientError`, prepare `LaunchError`, and vmnet runtime debug context until final display at `main`. `wrapper.rs` still stringifies when invoking `run` from wrapper mode because wrapper itself remains `Result<_, String>`.
- 2026-05-17 iteration 16 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; focused tests `payload_client_config_reports_structured_parse_errors` and `argv0_no_longer_selects_wrapper_or_tool_behavior`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 17 typed-error slice: converted `vm-frontend/src/appliance.rs` to `ApplianceError`/`ApplianceResult` for artifact manifest read/parse, repository-root inference, missing/invalid source hash metadata, changed source hashes, missing required inputs, and source file reads. Converted `vm-frontend/src/tls_bootstrap.rs` to `TlsBootstrapError`/`TlsBootstrapResult` for MITM CA directory creation, rcgen key/params/cert generation, certificate write, private-key write, and permission restriction. Existing callers still bridge these module errors to `String` while launch/wrapper seams remain stringly.
- 2026-05-17 iteration 17 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline appliance_source_hashes -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_mitm_ca -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed. `rg` confirms `appliance.rs` and `tls_bootstrap.rs` no longer expose `Result<_, String>`.
- 2026-05-17 iteration 18 typed-error slice: introduced `WrapperError`/`WrapperResult` in `vm-frontend/src/wrapper.rs`; `run_wrapper` now returns typed errors, preserves config and TLS bootstrap errors through `WrapperError` into top-level `CliError`, and delegates directly to `run_launch` for both plain and TUI modes instead of invoking top-level `run()` and stringifying `CliError`. `apply_configured_launch_defaults` and `config_share_shadow_backing_path` now return `WrapperResult` for typed config/path/share-shadow failures. Remaining known bridge: `parse_wrapper_args_with_terminal` still returns `String` for parser-focused tests and stringifies configured-default failures in that parser seam; `launch_cli.rs` and `self_test.rs` remain stringly.
- 2026-05-17 iteration 18 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline argv0_no_longer_selects_wrapper_or_tool_behavior -- --nocapture`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 19 typed-error slice: introduced `LaunchCliError`/`LaunchCliResult` in `vm-frontend/src/launch_cli.rs` and changed `run_launch` to return typed launch CLI errors. Top-level `CliError` and `WrapperError` now carry `LaunchCliError` transparently, so wrapper/plain launch dispatch no longer stringifies launch results at the boundary. `LaunchCliError` currently preserves `PayloadClientError`, `PayloadCliError`, and `ApplianceError`, plus a temporary `Message(String)` catch-all for still-stringly launch helpers.
- 2026-05-17 iteration 19 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 20 typed-error slice: pushed `LaunchCliError` deeper through `frontend_config_from_args`, `launch_payload_args`, `runtime_mounts`, `guest_payload_env`, `parse_guest_path_share`, and `parse_guest_path_share_shadow`. Added structured variants for frontend config loading, qemu/payload numeric parse errors, payload env format/empty key, empty payload script, share shadow parent/backing validation, share shadow backing mkdir, and `ConfigError` propagation. Remaining `LaunchCliError::Message(String)` catch-all is now concentrated around shared parse/path helpers, project lock/reset, local smoke upstream, readiness/no-net helpers, and final qemu status formatting.
- 2026-05-17 iteration 20 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline self_test_payload -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 21 reflection: accomplishments are substantial: `wra-9glk` and `wra-09v9` are closed after required validation, and typed errors now cover config, payload CLI, vmnet CLI, appliance freshness, TLS bootstrap, wrapper dispatch, top-level command dispatch, and most launch config/mount/env parsing. The seam-by-seam approach is working well: behavior-preserving changes have stayed small enough for focused tests, and repeated full vm-frontend offline validation remains green. The main remaining drag is diminishing returns in tiny shared helpers that still return `String` (`parse_clap_matches`, `absolute_cli_path`, AWS credential extraction, readiness probe formatting) plus `self_test.rs` internals. Adjust approach: avoid overfitting every helper before live validation; finish one more high-value launch slice, then run required validation and consider `wra-r070` closable if it passes.
- 2026-05-17 iteration 21 typed-error slice: converted project lock/reset, local HTTP smoke upstream setup, and no-net validation to typed `LaunchCliError` variants. `vmnet_cli.rs` now stringifies only when adapting the shared no-net validator to its own `VmnetCliError`.
- 2026-05-17 iteration 21 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; focused tests for `frontend_no_net`, `reset_project`, `wrapper_`, and `vmnet_gateway_runtime_config`; plus full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 22 validation: `./vm-frontend/validate.sh required` passed end-to-end for the typed-domain-errors slice, including docs/fmt/offline tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 22 ticket state: added final validation/remaining-seams note to `wra-r070` and closed it. Remaining stringly seams are intentionally narrow shared-helper/final-formatting adapters (`parse_clap_matches`, `absolute_cli_path`, AWS credential extraction, readiness probe text, and `self_test.rs` internals), while major command/config/launch/payload/vmnet paths now preserve typed errors to the binary boundary.
- 2026-05-17 iteration 23 ticket state: `tk ready` showed `wra-rjt4` ready after `wra-r070`; started `wra-rjt4` and added a planning note to keep user-facing CLI output separate from diagnostics.
- 2026-05-17 iteration 23 tracing slice: added direct workspace `tracing` dependency for `vm-frontend`; instrumented `vm-frontend/src/launch.rs` with structured spans/events around frontend startup, service readiness, QEMU spawn/PID/wait/exit/timeout, explicit termination, and unfinished-drop cleanup. Replaced diagnostic service-thread `eprintln!` failures for composed-fs/config-fs/vmnet with `tracing::error!` events. Intentional `launch: phase=...` stdout remains unchanged.
- 2026-05-17 iteration 23 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo check --manifest-path vm-frontend/Cargo.toml --offline`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline launch_ -- --nocapture` all passed.
- 2026-05-17 iteration 24 tracing slice: added structured command-dispatch events in `vm-frontend/src/main.rs`; added launch CLI lifecycle events in `vm-frontend/src/launch_cli.rs` for configured launch span, local smoke upstream, payload listener readiness, payload execution mode/exit code, and qemu success/timeout/failure; added docker unix proxy tracing in `vm-frontend/src/docker_proxy.rs` for service start, accepted clients, upstream connects, and client/accept failures. Intentional launch phase stdout remains unchanged, and guest-flush warning stderr remains while also emitting `tracing::warn!`.
- 2026-05-17 iteration 24 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_ -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline wrapper_ -- --nocapture` all passed.
- 2026-05-17 iteration 25 tracing slice: instrumented `vm-frontend/src/vmnet_runtime.rs` at runtime boundaries and event-log seams. `serve_vmnet_gateway` now creates a `vmnet.runtime` span and emits lifecycle events for socket bind/accept, host listener binding, and QEMU stream close/EOF. Gateway/proxy/host-ingress events now also emit `tracing` debug/warn events independently of whether the text event log file is enabled, with warnings for denials/failures/limits.
- 2026-05-17 iteration 25 subscriber decision: added a minimal env-controlled binary subscriber in `vm-frontend/src/main.rs` (`AGENTVM_LOG=error|warn|info|debug|trace`) rather than adding `tracing-subscriber`, avoiding offline dependency churn while making diagnostics visible on demand. Default CLI output remains unchanged when `AGENTVM_LOG` is unset.
- 2026-05-17 iteration 25 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline vmnet_ -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline argv0_no_longer_selects_wrapper_or_tool_behavior -- --nocapture`; and manual `AGENTVM_LOG=info target/debug/agentvm-frontend --help` all passed/behaved as expected.
- 2026-05-17 iteration 26 reflection: `wra-rjt4` now has structured tracing at command dispatch, launch supervision, launch CLI payload lifecycle, docker proxy, and vmnet runtime/event-log seams, with `AGENTVM_LOG` opt-in stderr diagnostics and default CLI output unchanged. The small boundary-first slices continue to work: focused tests and full vm-frontend offline tests remain green. Main tradeoff is the deliberately minimal in-tree subscriber rather than `tracing-subscriber`; this avoids offline dependency churn but is less featureful, which is acceptable for current diagnostics and can be revisited later if richer filtering/formatting is needed. Next priority is required live-capable validation and ticket closure if it passes without an appliance rebuild request.
- 2026-05-17 iteration 26 validation: broader vm-frontend offline validation passed: `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` and `cargo test --manifest-path vm-frontend/Cargo.toml --offline`.
- 2026-05-17 iteration 27 validation: `./vm-frontend/validate.sh required` passed end-to-end for the tracing slice, including docs/fmt/offline tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 27 ticket state: added final validation note to `wra-rjt4` and closed it.
- 2026-05-17 iteration 28 ticket state: `tk ready` showed `wra-n0fe` unblocked after `wra-rjt4`; started `wra-n0fe` and added a planning note to keep the first slice minimal, preserving current launch behavior while introducing the Tokio edge/supervisor skeleton.
- 2026-05-17 iteration 28 Tokio/supervisor slice: added Tokio as a direct workspace dependency for `vm-frontend` with application-edge/supervision features and fetched it for offline validation. Production `main` now constructs a multi-thread Tokio runtime and blocks on `run_cli_async`, which delegates to the existing synchronous command dispatch so behavior remains unchanged. Added `vm-frontend/src/supervisor.rs` with `LaunchSupervisor`, typed task names/kinds/status/results, watch-channel shutdown/cancellation, and explicit blocking service classification for composed-fs/config-fs.
- 2026-05-17 iteration 28 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervisor -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline argv0_no_longer_selects_wrapper_or_tool_behavior -- --nocapture` all passed.
- 2026-05-17 iteration 29 supervisor ownership/readiness slice: extended `LaunchSupervisor` with per-task watch-backed status state, `task_status`/`task_statuses` queries, task status subscriptions, and `SupervisorTaskController` handles for marking starting/ready/finished/failed/cancelled with typed errors. This gives future supervised blocking services, vmnet, and qemu owners explicit readiness/failure/cancellation reporting without converting internals yet.
- 2026-05-17 iteration 29 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervisor -- --nocapture` passed.
- 2026-05-17 iteration 30 supervisor run-plan slice: added no-spawn `run_until_shutdown` API that waits for supervisor cancellation, preserves terminal task statuses, and marks unfinished tasks cancelled with the shutdown reason. Decided this skeleton is sufficient for `wra-n0fe` pending live-capable validation: the async binary edge exists, supervisor APIs expose typed task ownership/readiness/failure/cancellation, and no long-lived services are newly detached or converted internally.
- 2026-05-17 iteration 30 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervisor -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 31 reflection: `wra-n0fe` has met the intended foundational scope: the binary enters through a Tokio runtime, launch command behavior remains synchronous/unchanged for now, and `LaunchSupervisor` exposes typed task specs, per-task status/watch subscriptions, controller handles, shutdown cancellation, and a no-spawn run summary. The minimal-skeleton-first approach is working because it creates stable ownership/readiness/failure seams without moving composed-fs/vmnet work onto Tokio core workers. Main tradeoff is that current production `launch.rs` still owns detached thread spawning; deeper conversion belongs to follow-on tickets like `wra-zqci`/`wra-gq8e`. Next step is required live-capable validation and ticket closure if it passes without an appliance rebuild request.
- 2026-05-17 iteration 31 validation: `./vm-frontend/validate.sh required` passed end-to-end for the Tokio edge/supervisor skeleton, including docs/fmt/offline tests, offline guest service tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 31 ticket state: added final validation/scope note to `wra-n0fe` and closed it.
- 2026-05-17 iteration 32 ticket state: `tk ready` showed protocol/shared `wra-cvmy` ready after `wra-9glk`; started `wra-cvmy` and added a planning note for a behavior-preserving shared crate extraction with fuzz coverage.
- 2026-05-17 iteration 32 protocol extraction: added workspace crate `agentvm-payload-protocol`, moved the guest-service frame protocol implementation/tests there, made `guest-service` re-export the shared crate, updated `vm-frontend/src/payload_client.rs` to use shared frame encode/header-decode/size-limit semantics, and added `payload_protocol_frame` fuzz target coverage.
- 2026-05-17 iteration 32 validation: `cargo fmt --manifest-path payload-protocol/Cargo.toml`; `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path payload-protocol/Cargo.toml --offline`; `cargo test --manifest-path guest-service/Cargo.toml --offline`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client::tests::frame -- --nocapture`; and `cargo check --manifest-path vm-frontend/fuzz/Cargo.toml --offline --bin payload_protocol_frame` all passed.
- 2026-05-17 iteration 33 protocol extraction: moved `PayloadRequest`, `DiagnosticRequest`, `PayloadEvent`, exit/signal/resize payload schemas, event decoding, and signal/resize JSON payload builders into `agentvm-payload-protocol`; `vm-frontend/src/payload_client.rs` now re-exports the moved public request/event types to preserve existing module API while using shared event/control-frame decoding.
- 2026-05-17 iteration 33 async API slice: added Tokio-ready `read_frame_async`/`write_frame_async` helpers in `agentvm-payload-protocol`. Async read decodes and validates the 5-byte header before allocating payload storage; async write rejects oversized public `Frame` payloads before encoding.
- 2026-05-17 iteration 33 validation: `cargo fmt --manifest-path payload-protocol/Cargo.toml`; `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path payload-protocol/Cargo.toml --offline`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture`; `cargo check --manifest-path vm-frontend/fuzz/Cargo.toml --offline --bin payload_protocol_frame`; and `cargo test --manifest-path guest-service/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 34 validation-gate update: added `payload-protocol` to `vm-frontend/validate.sh` fast/fmt coverage, updated `AGENTS.md` and `vm-frontend/validation-workflow.md`, and documented the new `payload_protocol_frame` fuzz target. No config semantics changed.
- 2026-05-17 iteration 34 broader validation: `cargo fmt --manifest-path vm-frontend/fuzz/Cargo.toml`; `./vm-frontend/validate.sh docs`; `./vm-frontend/validate.sh fmt`; `./vm-frontend/validate.sh fast`; and `./vm-frontend/validate.sh fuzz-check` all passed after the validation-gate/docs update.
- 2026-05-17 iteration 34 required validation: `./vm-frontend/validate.sh required` passed end-to-end for `wra-cvmy`, including updated `payload-protocol` fmt/offline tests, docs/fmt/offline tests, offline guest service tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 34 ticket state: added final validation/scope note to `wra-cvmy` and closed it.
- 2026-05-17 iteration 35 ticket state: `tk ready` showed `wra-jkeg` unblocked after `wra-cvmy`/`wra-n0fe`; started `wra-jkeg` and added a planning note to keep the first slice additive, focused on async payload client/event plumbing rather than the full TUI control-socket frontend owned by `wra-yl7i`.
- 2026-05-17 iteration 35 async payload slice: added `AsyncPayloadSession` in `vm-frontend/src/payload_client.rs`. It writes the initial `RUN_PRIMARY` request with shared protocol codecs, owns split Tokio read/write halves, accepts bounded `mpsc` commands for input/signal/resize, publishes payload events/errors through a bounded async receiver, and aborts reader/writer tasks on drop. Existing synchronous payload runner and TUI paths remain unchanged.
- 2026-05-17 iteration 35 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_payload_session -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture` all passed.
- 2026-05-17 iteration 36 reflection: The foundational option-1 path is working: workspace/module/error/tracing/Tokio-edge/protocol tickets are closed after required validation, and `wra-jkeg` now has additive async payload session plumbing without disturbing synchronous launch/TUI behavior. The boundary-first approach continues to be effective because shared codecs and focused async tests allow Tokio-owned I/O seams to land incrementally. Main risk is scope creep: full TUI control-socket semantics belong to `wra-yl7i`, so `wra-jkeg` should finish reusable async payload primitives and a minimal compatibility seam rather than rewrite the whole UI/runtime integration now.
- 2026-05-17 iteration 36 async helper slice: added `AsyncPayloadSession::cancel()`, `ping_payload_async_io()`, and `run_diagnostic_async_io()` with `tokio::time::timeout`, async output streaming, exit/failure handling, and partial-frame deadline behavior.
- 2026-05-17 iteration 36 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_ -- --nocapture`; and `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture` all passed.
- 2026-05-17 iteration 37 TUI bridge compatibility slice: added cloneable `AsyncPayloadCommandSender` as a minimal seam for synchronous TUI/control frontends to feed the async payload writer using bounded channels. It provides async send, nonblocking `try_send`, sync `blocking_send`, and input/signal/resize convenience methods; full TUI control-socket semantics remain scoped to `wra-yl7i`.
- 2026-05-17 iteration 37 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_payload -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline payload_client -- --nocapture`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` all passed.
- 2026-05-17 iteration 37 decision point: `wra-jkeg` is not ready to close yet because production launch/TUI still use the synchronous payload path. Next decision is whether to wire async sessions into production now or keep this ticket open until integration work lands through `wra-zqci`/`wra-yl7i`.
- 2026-05-17 iteration 38 scope decision: accepted `wra-jkeg` as reusable async payload client/session plumbing rather than production launch/TUI wiring. The later ticket note and dependency graph place production async launch-supervisor integration in `wra-zqci` and full TUI/control-socket frontend behavior in `wra-yl7i`; `wra-jkeg` provides the async session/client primitives and bridge seam they can consume.
- 2026-05-17 iteration 38 required validation: `./vm-frontend/validate.sh required` passed end-to-end, including docs/fmt/offline tests, fuzz target compilation, `live-smoke`, and `live-setup-tools`; no appliance rebuild was requested.
- 2026-05-17 iteration 38 ticket state: added final validation/scope note and closed `wra-jkeg`.
- 2026-05-17 iteration 39 ticket state: `tk ready` showed `wra-zqci` ready after `wra-jkeg`/`wra-n0fe` groundwork; started `wra-zqci` and added a planning note.
- 2026-05-17 iteration 39 inspection: current launch still centers on synchronous `start_frontend_with_policy`: blocking `mkfs.ext4`, detached std service threads, `wait_for_path` sleep polling for sockets, optional docker proxy startup, and std QEMU process ownership. `RunningFrontend` owns QEMU but not service joins/failure propagation; `LaunchSupervisor` status/controller APIs are present but not yet wired into launch.
- 2026-05-17 iteration 40 async process slice: added additive Tokio QEMU/process helpers in `vm-frontend/src/launch.rs` without changing production launch behavior. New helpers use `tokio::process::Command`/`Child` and `tokio::time::timeout` while preserving `QemuExit` semantics and kill-on-timeout behavior.
- 2026-05-17 iteration 40 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_qemu -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline launch_ -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 1 supervised QEMU slice: added `run_supervised_qemu_process_async`, which validates the QEMU controller, marks starting before async spawn, ready after spawn, finished on normal exit, and failed on timeout/spawn/wait errors while preserving `QemuExit` return semantics. `spawn_qemu_process_async` now creates the qemu-log parent directory for self-contained async helper use.
- 2026-05-17 continued iteration 1 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_async_qemu -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_qemu -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline launch_ -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 2 async readiness slice: added `wait_for_path_async` and `wait_for_service_ready_async` in `vm-frontend/src/launch.rs`. The async path helper mirrors synchronous `wait_for_path` with Tokio sleep/timeout boundaries, and the service readiness helper marks supervisor service controllers ready or failed on timeout. The synchronous timeout formatting now shares `path_wait_timeout` to preserve messages.
- 2026-05-17 continued iteration 2 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_service_readiness -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_qemu -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 3 state-writing QEMU runner slice: added `run_supervised_qemu_process_with_state_async`, which publishes QEMU supervisor statuses and writes `state.json` as `running` after async spawn, then `exited` or `timed_out` with `qemu_status` after wait/timeout. Shared supervised QEMU wait/status logic now lives in `finish_supervised_qemu_wait`.
- 2026-05-17 continued iteration 3 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_async_qemu -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_service_readiness -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 4 supervised blocking service slice: added `spawn_supervised_blocking_service_until_ready`, which marks service tasks starting, runs blocking service startup through `tokio::task::spawn_blocking`, races readiness against early service exit, returns the service join handle after readiness, and marks failed on timeout or early service failure. This preserves the intended bounded-blocking service model for composed-fs/config-fs/vmnet integration.
- 2026-05-17 continued iteration 4 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline async_service_readiness -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_async_qemu -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 5 service handle slice: `spawn_supervised_blocking_service_until_ready` now returns `SupervisedBlockingService`, retaining the service controller and join handle. `SupervisedBlockingService::wait` marks services finished on clean exit and failed on late service error or join failure, giving the future async runner a typed post-readiness service-failure observation point.
- 2026-05-17 continued iteration 5 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_async_qemu -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 6 async runner skeleton: added `run_supervised_qemu_process_with_service_async`, combining state-writing async QEMU ownership with a post-readiness supervised blocking service handle. It races QEMU exit/timeout against service completion, kills QEMU on service failure, marks QEMU failed with service context, writes `state.json` as `service_failed`, and returns the service error.
- 2026-05-17 continued iteration 6 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_qemu_with_service -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_async_qemu -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 7 multi-service runner slice: added `run_supervised_qemu_process_with_services_async` and `wait_for_first_service_completion`, so multiple `SupervisedBlockingService` handles can be monitored concurrently while QEMU runs. The first service completion/failure terminates QEMU, marks QEMU failed, writes `state.json` as `service_failed`, and returns the service cause. The one-service runner now delegates to this multi-service helper.
- 2026-05-17 continued iteration 7 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_qemu_with_service -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_qemu_with_services -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 8 async production-shape launch slice: added `run_frontend_until_qemu_exit_with_policy_and_timeout_async`. It performs launch preparation/state using existing config/mount/policy code, creates a `LaunchSupervisor`, starts composed-fs/config-fs/vmnet through supervised `spawn_blocking` service readiness helpers, waits for optional docker proxy readiness asynchronously, and runs QEMU through the multi-service supervised runner. Production `start_frontend_with_policy` remains unchanged.
- 2026-05-17 continued iteration 8 monitor cleanup: changed multi-service monitoring to use `tokio::task::JoinSet` rather than detached monitor tasks, so dropping the monitor future aborts pending monitor tasks.
- 2026-05-17 continued iteration 8 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_qemu_with_services -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_blocking_service -- --nocapture`; `cargo check --manifest-path vm-frontend/Cargo.toml --offline`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 9 production-routing decision: keep production launch CLI on the live-validated synchronous path for now. Although `run_frontend_until_qemu_exit_with_policy_and_timeout_async` constructs real services and QEMU, routing production through it should wait until async shutdown/drop semantics for all service tasks are explicit, including expected service exits after QEMU shutdown and monitor/task lifecycle cleanup.
- 2026-05-17 continued iteration 9 broader validation: full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` passed for the current `wra-zqci` async slices.
- 2026-05-17 continued iteration 10 lifecycle cleanup: `wait_for_qemu_or_service_failure` now snapshots service controllers before monitoring. On normal QEMU exit it marks unfinished services cancelled with reason `qemu exited`; on QEMU timeout with `qemu timed out`; and on first service failure it marks peer services cancelled with reason `peer service failed`. Added tests for QEMU-exit cancellation and peer-service cancellation on first service failure.
- 2026-05-17 continued iteration 10 validation: full `cargo test --manifest-path vm-frontend/Cargo.toml --offline` and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` passed.
- 2026-05-17 continued iteration 11 reflection: `wra-zqci` now has an additive async production-shape runner with supervised QEMU, real launch prep/service construction, multi-service failure racing, state updates, and cancellation status for normal QEMU exit, timeout, and peer service failure. Small focused helper surfaces plus unit tests are working well. Main blocker: production CLI is still deliberately on the synchronous path because the async helper has not been live-validated and service shutdown is status-level only for blocking services; `spawn_blocking` cannot forcibly stop a running composed-fs/vmnet loop.
- 2026-05-17 continued iteration 11 lifecycle test: added coverage for QEMU timeout cancelling unfinished service statuses with reason `qemu timed out`.
- 2026-05-17 continued iteration 11 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline supervised_qemu -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 12 production routing: direct no-payload/plain `agentvm launch` now routes through an async-aware `run_async`/`run_launch_async` path and the additive async launch runner. TUI/payload launches delegate to existing synchronous `run_launch`, and wrapper launches remain on the existing synchronous wrapper path to keep payload/TUI lifecycle changes out of `wra-zqci`.
- 2026-05-17 continued iteration 12 validation: `cargo fmt --manifest-path vm-frontend/Cargo.toml`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline argv0_no_longer_selects_wrapper_or_tool_behavior -- --nocapture`; `cargo test --manifest-path vm-frontend/Cargo.toml --offline frontend_ -- --nocapture`; and `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 13 validation: `cargo test --manifest-path vm-frontend/Cargo.toml --offline`; `cargo fmt --manifest-path vm-frontend/Cargo.toml -- --check`; and `./vm-frontend/validate.sh required` all passed. Required validation did not request an appliance rebuild.
- 2026-05-17 continued iteration 13 ticket lifecycle: closed `wra-zqci` after required validation and started `wra-662v`.
- 2026-05-17 continued iteration 13 `wra-662v` reconnaissance: `guest-service/src/main.rs` is still a skeleton; `guest-service/src/lib.rs` only re-exports `agentvm_payload_protocol`; Python baseline in `docker/guest-payload-server.py` provides TCP serving, ping, one primary payload session, bounded diagnostic concurrency, PTY/process-group execution, stdin/signal/resize handling, diagnostic timeout/output caps, initial/session I/O timeouts, client/session caps, and failure frames.
- 2026-05-17 continued iteration 14 `wra-662v` implementation: added the first Rust guest-service core in `guest-service/src/lib.rs` around shared protocol initial-frame handling and admission limits. The service still returns explicit not-implemented failure frames for actual primary/diagnostic execution, preserving opt-in/non-default status while creating a tested place to add execution behavior.
- 2026-05-17 continued iteration 14 validation: `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture`; and `cargo fmt --manifest-path guest-service/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 15 `wra-662v` implementation: Rust guest-service diagnostics now execute real shell commands with Tokio process I/O, bounded output streaming, timeout/truncation kills, and frontend-compatible diagnostic exit JSON. Primary payload execution remains an explicit not-implemented failure.
- 2026-05-17 continued iteration 15 validation: `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture`; and `cargo fmt --manifest-path guest-service/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 16 reflection: `wra-662v` is progressing best as small opt-in Rust guest-service surfaces: protocol/admission first, diagnostics second, TCP serving/CLI third. Shared `payload-protocol` keeps frontend/server frame semantics aligned, and fast Tokio duplex/listener tests provide useful feedback. The primary blocker/risk remains primary payload parity: PTY, controlling terminal, process groups, stdin/signal/resize, slow client cleanup, and UID/GID/HOME behavior.
- 2026-05-17 continued iteration 16 implementation: added `serve_tcp`/`serve_listener`, typed TCP bind/accept errors, a real `agentvm-guest-service --tcp-port ...` CLI with Python-compatible serving flags, CLI parser tests, and a loopback ping listener test. The default appliance path remains unchanged.
- 2026-05-17 continued iteration 16 validation: `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture`; and `cargo fmt --manifest-path guest-service/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 17 `wra-662v` implementation: added opt-in appliance plumbing for the Rust guest service. `docker/guest-init.sh` keeps Python default but supports `agentvm_payload_service=rust` with fail-closed executable checks; `docker/build-appliance.sh` supports `AGENTVM_PAYLOAD_SERVICE=rust` and emits the matching kernel cmdline only when requested. This changes appliance source inputs, so required validation/closure will likely require rebuilding appliance artifacts with updated hashes.
- 2026-05-17 continued iteration 17 validation: `sh docker/tests/test_guest_init.sh`; `bash docker/tests/test_build_appliance.sh`; `cargo fmt --manifest-path guest-service/Cargo.toml -- --check`; and `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture` all passed.
- 2026-05-17 continued iteration 18 `wra-662v` implementation: added initial non-PTY primary payload execution in Rust guest-service, including piped stdio, output frames, exit frames, stdin forwarding, signal parsing/delivery to child PID, resize parsing as no-op, and child termination on disconnect/write failure. Added `libc` for signal delivery.
- 2026-05-17 continued iteration 18 validation: `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture`; and `cargo fmt --manifest-path guest-service/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 19 `wra-662v` implementation: primary/diagnostic child commands now use `setsid()` and process-group signal delivery. Primary control EOF/error now terminates the group once and disables further reads to avoid busy-looping on disconnect. Added tests for signal forwarding and disconnect cleanup.
- 2026-05-17 continued iteration 19 validation: `cargo fmt --manifest-path guest-service/Cargo.toml`; `cargo test --manifest-path guest-service/Cargo.toml --offline guest_service_primary -- --nocapture`; `cargo test --manifest-path guest-service/Cargo.toml --offline -- --nocapture`; and `cargo fmt --manifest-path guest-service/Cargo.toml -- --check` all passed.
- 2026-05-17 continued iteration 20 broad validation: `sh docker/tests/test_guest_init.sh`; `bash docker/tests/test_build_appliance.sh`; `cargo test --workspace --offline`; and `cargo fmt --all -- --check` passed.
- 2026-05-17 continued iteration 20 required validation attempt #1: `./vm-frontend/validate.sh required` failed in vm-frontend offline tests due flaky `launch::tests::supervised_blocking_service_marks_failed_after_readiness`. Created bug `wra-kolb`, fixed the test race, and focused validation passed.
- 2026-05-17 continued iteration 20 required validation attempt #2: `./vm-frontend/validate.sh required` timed out at the command timeout after reporting `payload_client::tests::payload_session_runner_cancel_token_interrupts_blocked_receive` running for over 60 seconds. Focused rerun of that test passed immediately. Created bug `wra-cqjc` for investigation. Required validation did not complete and did not reach any appliance rebuild request.
- 2026-05-17 second continuation iteration 1 `wra-cqjc` hardening: `PayloadCancelToken::register_stream` now immediately shuts down a stream when cancellation already happened, closing a pre-registration cancellation race. The blocking-receive cancellation test now uses bounded waits/timeouts and asserts client close, so failures should not hang validation indefinitely.
- 2026-05-17 second continuation iteration 1 validation: focused payload runner tests passed, then `cargo test --workspace --offline` and `cargo fmt --all -- --check` passed.
- 2026-05-17 second continuation iteration 1 ticket lifecycle: closed `wra-kolb` after focused and broad validation. `wra-cqjc` remains open until required validation completes without the prior hang.
- 2026-05-17 second continuation iteration 2 required validation: `./vm-frontend/validate.sh required` progressed past the prior payload cancellation hang and into live-smoke. It stopped with stale appliance artifacts because `docker/build-appliance.sh` changed after `docker/out/artifact-manifest.json` was generated. Rebuild requested: `sudo ./docker/build-appliance.sh` to refresh `docker/out/artifact-manifest.json`, `docker/out/rootfs.raw`, `docker/out/initrd.img`, and `docker/out/vmlinuz`; for Rust opt-in appliance validation use `AGENTVM_PAYLOAD_SERVICE=rust AGENTVM_GUEST_SERVICE_BIN=<built agentvm-guest-service>`.
- 2026-05-17 second continuation iteration 3 `wra-gq8e`: added tested async Docker Unix proxy entrypoint with Tokio accept/connect/copy, watch shutdown, JoinSet client ownership, and bounded connection limit. Existing sync launch behavior is unchanged pending supervision integration.
- 2026-05-17 second continuation iteration 4 `wra-gq8e`: async launch now uses the supervised Tokio Docker proxy when Docker API forwarding is present. Added optional supervisor task metadata and async service shutdown signaling from QEMU lifecycle. Remaining sync Docker proxy path is still present for non-async launch and keeps `wra-gq8e` open.
- 2026-05-17 second continuation iteration 5 `wra-gq8e`: removed legacy sync Docker proxy per-client copy threads; sync entrypoint now reuses the async implementation on a current-thread runtime after synchronous bind. Offline vm-frontend validation passed, but ticket closure still awaits required/live-capable validation after appliance rebuild.
- 2026-05-17 second continuation iteration 6 `wra-73tn`: added initial `VmnetCore` boundary around `VmnetGateway`, moved stream helper APIs to accept the core, and added socket-free core behavior coverage. This is intentionally preparatory and does not move runtime I/O onto Tokio yet.
- 2026-05-17 second continuation iteration 7 `wra-73tn`: moved deferred-DNS service owner helpers and the production vmnet loop's guest-frame/DNS paths behind `VmnetCore`. Full vm-frontend offline validation passed.
- 2026-05-17 second continuation iteration 8 `wra-73tn`: moved proxy pump and TCP connect service owner helpers to take `VmnetCore`, reducing raw `VmnetGateway` exposure in production runtime code. Full vm-frontend offline validation passed.
- 2026-05-17 second continuation iteration 9 `wra-73tn`: moved host-ingress pump/open helper seams to `VmnetCore` and centralized session opening through `open_host_ingress_session`. Full vm-frontend offline validation passed.
- 2026-05-17 second continuation iteration 10 `wra-73tn`: made remaining `VmnetCore` gateway escape hatch private and documented it as adapter-only. Full vm-frontend offline validation passed.
- 2026-05-17 second continuation iteration 11 broad validation: `cargo test --workspace --offline` and `cargo fmt --all -- --check` passed after the accumulated `wra-662v`, `wra-gq8e`, and `wra-73tn` changes. Required validation remains blocked by stale appliance artifacts and was not retried.
- 2026-05-17 second continuation iteration 12 self-test cleanup: started `wra-6n71`, moved self-test-specific tests into `vm-frontend/src/self_test.rs`, and moved default artifact-manifest path helpers to `launch_cli.rs`. Focused validation passed.
- 2026-05-17 second continuation iteration 12 required validation: after user-confirmed appliance rebuild, `./vm-frontend/validate.sh required` passed including `live-smoke` and `live-setup-tools`; `wra-73tn` was closed. The rebuilt manifest is default Python (no `agentvm_payload_service=rust`), so `wra-662v` remains open; `wra-gq8e` remains open pending Docker-proxy live coverage.
- 2026-05-17 second continuation iteration 13 Docker live validation: `./vm-frontend/validate.sh live-docker` passed, covering Docker egress allow, no-net egress denial, and host-to-container published-port access. Closed `wra-gq8e` after this scenario plus iteration 12 required validation.
- 2026-05-17 second continuation iteration 14 self-test extraction: added `vm-frontend/src/self_test_payload.rs` for guest payload script generation and hostile payload steps; focused/full vm-frontend offline validation passed.
- 2026-05-17 second continuation iteration 14 required validation: after the user's rebuild, `./vm-frontend/validate.sh required` passed. The artifact manifest remains default Python, so this does not close Rust opt-in appliance validation for `wra-662v`.
- 2026-05-17 second continuation iteration 15 `wra-6n71`: production `agentvm` no longer exposes `self-test`; `validate.sh` and docs now use `--bin agentvm-frontend -- self-test`; `./vm-frontend/validate.sh required` passed and `wra-6n71` was closed.
- 2026-05-17 second continuation iteration 16 `wra-1esa`: started ticket and removed wrapper compatibility pre-scan for `--docker`, `--docker-machine`, and `--pass-env`; removed flags now fail as normal unknown arguments. Full vm-frontend offline validation passed.

## Notes
- Complete-epic continuation iteration 1: user rebuilt the appliance, but the manifest is still Python-default. Built `target/debug/agentvm-guest-service`; attempted non-interactive Rust opt-in rebuild with `sudo -n env AGENTVM_PAYLOAD_SERVICE=rust AGENTVM_GUEST_SERVICE_BIN=$(pwd)/target/debug/agentvm-guest-service ./docker/build-appliance.sh`, but sudo requires a password. User-side command needed for Rust opt-in validation: `sudo env AGENTVM_PAYLOAD_SERVICE=rust AGENTVM_GUEST_SERVICE_BIN=/home/hsaksela/ai/wrapper/target/debug/agentvm-guest-service ./docker/build-appliance.sh`. Continue other epic work while blocked; do not close `wra-662v` as Rust-live-validated or switch default.
- Second continuation iteration 20 final handoff: do not start broad implementation at loop end. Remaining option-1 children are `wra-662v` in progress plus open `wra-f762`, `wra-yl7i`, and `wra-0a0r`. `wra-662v` is the strategic blocker: current required validation is Python-default and does not live-validate the Rust opt-in appliance. Next decision is opt-in Rust appliance rebuild/live-smoke versus explicitly leaving all Rust live parity/default-switch validation to `wra-y335`.
- Second continuation iteration 19 closed `wra-1esa` after `./vm-frontend/validate.sh required` passed. Final slice removed remaining wrapper parser/runner program-name plumbing: top-level dispatch still uses `argv0=agentvm` to choose wrapper mode, but wrapper parsing no longer accepts or interprets executable names. Command-line compatibility aliases/shims are gone while `.sandbox/config.json` migration remains intact.
- Second continuation iteration 18 made the first wrapper-to-launch typed-construction slice for `wra-1esa`: wrapper-emitted launch flags now go through a local `WrapperLaunchFlag` enum plus push/has/upsert helpers, while serialization remains at the existing `run_launch` argv boundary. This centralizes flag spelling without changing config semantics or introducing a broad launch-options layer.
- Second continuation iteration 17 removed the explicit `wrap` compatibility subcommand: `agentvm-frontend wrap` is now an unknown command, `agentvm wrap` is an invalid wrapper argument, help/internal wrapper display no longer advertises that compatibility path, and `requirements.md` marks it unsupported. Continue `wra-1esa` by reducing stringly wrapper-to-launch construction rather than preserving more CLI aliases; config-file migration remains untouched.
- Second continuation iteration 16 selected `wra-1esa` because `wra-662v` is blocked on privileged Rust opt-in appliance validation. This is a useful stopgap only if it stays deletion-focused and does not grow a new CLI abstraction prematurely.
- Second continuation iteration 15 completed that entrypoint ownership change for `wra-6n71`: validation scenarios are now explicitly frontend validation commands rather than production `agentvm` wrapper commands. This leaves `wra-662v` as the main open option-1 work in this continuation unless a new dependency-ready ticket is selected.
- Second continuation iteration 14 confirms `wra-6n71` should be driven toward a real entrypoint ownership change, not just more file splitting. The next self-test slice should likely add a dedicated validation harness binary or route `validate.sh` away from the production `agentvm self-test` command while preserving scenario behavior.
- Second continuation iteration 13 resolved the Docker-proxy validation gap with `live-docker` and closed `wra-gq8e`. The remaining strategic fork is `wra-662v`: either validate the Rust opt-in appliance path directly or explicitly leave live parity/default switch to `wra-y335` and move on to `wra-6n71`.
- Second continuation iteration 12 unblocked the default required gate and closed the vmnet boundary ticket, but it also clarified that not all validation-blocked work is equally covered by `required`: Docker proxy needs a Docker-specific live scenario, and Rust guest-service needs an opt-in appliance build/live path or explicit deferral to `wra-y335`.
- Second continuation iteration 11 confirms the current code is broadly healthy offline. Avoid continuing to pile on refactors only because live validation is blocked; prefer either a clearly bounded next ticket or wait for appliance rebuild to close/validate the in-progress work.
- Second continuation iteration 10 tightened the boundary instead of broadening the refactor, which matches the “more code is a liability” guidance. Avoid forcing a Tokio vmnet rewrite in this ticket; the current boundary is useful preparation for later driver work.
- Second continuation iteration 9 completed the host-ingress seam enough for now. Remaining gateway access is mostly within adapter helpers; next slice should tighten or document that boundary rather than broaden the refactor.
- Second continuation iteration 8 completed the proxy/TCP-connect seam enough for now. The remaining high-value seam is host ingress, after which `gateway_mut` should ideally be confined to a few core adapter helpers rather than production loop code.
- Second continuation iteration 7 made `VmnetCore` more than a test wrapper by routing production guest-frame/DNS behavior through it. The next good seam is the proxy/TCP-connect side; keep the edits mechanical and leave runtime I/O synchronous for this ticket.
- Second continuation iteration 6 kept momentum without fighting the stale appliance blocker. The `VmnetCore` wrapper is small but important: future Tokio drivers can target it while smoltcp/gateway ordering remains synchronous and single-owner.
- Second continuation iteration 5 reflection: the continuation has shifted from `wra-662v` feature work to validation/blocker management plus independent Tokio-boundary refactors. This is the right course while appliance artifacts are stale. Avoid closing tickets prematurely; continue with small code-reducing slices and focused validation.
- Second continuation iteration 4 completed the wiring/cancellation slice for `wra-gq8e`. Next slice should reduce code by retiring the legacy sync Docker proxy path where possible; more side-by-side code would be counter to the architecture goal.
- Second continuation iteration 3 made useful progress while appliance artifacts are stale by starting `wra-gq8e`. Keep the next slice focused on wiring/cancellation/supervisor semantics rather than a broad Docker proxy rewrite.
- Second continuation iteration 2 converted the blocker from validation flake to explicit appliance freshness. Do not retry required validation or close `wra-662v` until the user confirms rebuilding the appliance artifacts from the changed scripts/source hashes.
- Second continuation iteration 1 focused on validation stability rather than new feature breadth. The `wra-cqjc` hardening addresses a real cancellation race and makes the regression test bounded. Next priority is rerunning required validation; if it reaches appliance freshness and requests a rebuild, stop and ask the user for the rebuild rather than closing `wra-662v`.
- Continued iteration 20 validated broadly and attempted required validation, but `wra-662v` cannot close: one validation flake was fixed/documented as `wra-kolb`, then required validation timed out on an intermittent payload cancel-token test documented as `wra-cqjc`. Since required validation did not finish, no appliance rebuild request was observed yet, although appliance script changes still make a rebuild likely before final closure.
- Continued iteration 19 deepened primary cleanup semantics without taking on PTY yet. This was a good small slice because process groups and disconnect cleanup are required even after PTY support lands. Next iteration should either add one more bounded parity feature or pause and run broader validation/document explicit gaps rather than overreaching into a full PTY implementation.
- Continued iteration 18 began primary execution with a non-PTY Tokio runner. This is useful as a stepping stone but not enough for full parity; the next decision should be whether to deepen primary semantics (process group/session cleanup, then PTY) or stop this ticket with explicit gaps and leave parity/default gating to `wra-y335`.
- Continued iteration 17 added opt-in appliance plumbing before primary PTY/control work. This is useful because the Rust service now has an actual appliance activation path for future parity/live validation, while default Python behavior is unchanged. Caveat: because appliance source scripts changed, required validation will likely ask for a rebuilt appliance before `wra-662v` can close.
- Continued iteration 16 made the Rust guest-service binary reachable via a Tokio TCP listener and CLI. Approach remains sound: keep the Rust service opt-in and tested in layers before touching primary/default behavior. Next decision: primary payload PTY/control handling versus opt-in appliance wiring for the already reachable diagnostic-capable service.
- Continued iteration 15 implemented diagnostics first, validating Tokio child process, stdout/stderr streaming, timeout, truncation, and exit-frame behavior without taking on primary PTY/control-channel complexity yet. The remaining large `wra-662v` choices are TCP serving/CLI packaging versus primary payload execution; both must keep Python as default until `wra-y335`.
- Continued iteration 14 implemented the first small Rust guest-service core with admission guards and initial-frame protocol handling. Next useful slice is likely diagnostics, because it can be implemented and validated without PTY/control-channel complexity while still exercising Tokio process/timeout/output-limit behavior.
- Continued iteration 13 closed `wra-zqci` after broader offline and required live-capable validation passed. Started `wra-662v`; next work should avoid immediately cloning the whole Python service and instead build a small tested Rust/Tokio guest-service core around the shared protocol, preserving opt-in status until `wra-y335` parity validation.
- Continued iteration 12 routed the direct no-payload/plain launch path through the async runner while deliberately leaving payload/TUI and wrapper launch behavior synchronous. Next priority is broader offline validation, then required live-capable validation if the route is stable enough to close `wra-zqci`.
- Continued iteration 11 reflection/adjustment: stop expanding async launch API breadth and focus on production-routing readiness. The next useful step is deciding whether to route the no-payload launch path through the async runner, then run required live-capable validation if closing `wra-zqci`; otherwise document the remaining blocker explicitly in the ticket.
- Continued iteration 10 tightened async service lifecycle status semantics and broad offline validation remains green. The async launch path is closer to production routing readiness; next priority is deciding whether to route non-TUI run-until-exit through it behind the existing Tokio CLI boundary, then run required live-capable validation if closing `wra-zqci`.
- Continued iteration 9 decided not to switch production launch CLI yet. The async path has enough pieces to start real services/QEMU, but it needs explicit shutdown/drop semantics before replacing the live-validated synchronous path. Broader vm-frontend offline validation is green; next priority is async lifecycle cleanup, then production routing/required validation.
- Continued iteration 8 connected real launch preparation/service construction to the async runner via an additive function while keeping production launch unchanged. Next priority is deciding the safest production routing path: either use the async launch function in the non-TUI run-until-exit path first, or add more tests around service-failure/timeout state before switching CLI behavior.
- Continued iteration 7 generalized the runner skeleton to multiple service handles. Next priority is less about adding more standalone helpers and more about connecting this runner to real launch preparation/service construction in a controlled way, likely behind an additive async launch function before replacing production `start_frontend_with_policy`.
- Continued iteration 6 reflection: the additive approach remains effective: supervised async process, readiness, state-writing, blocking service handles, and now one-service QEMU/service failure racing all have focused tests. The next adjustment should be to consolidate these helpers into a small multi-service runner API rather than adding more one-off helpers, then decide when to wire production launch.
- Continued iteration 5 reflection: `wra-zqci` is progressing through additive, focused async seams without changing production launch behavior. The main remaining gap is combining these pieces into one async launch runner that starts services, observes post-readiness service failures while QEMU runs, terminates QEMU on service failure, and records shutdown cause in state.
- Continued iteration 4 added a supervised blocking-service readiness helper that keeps service execution on `spawn_blocking` and publishes readiness/failure through `LaunchSupervisor`. Next work should use this helper in an async launch-runner skeleton for composed-fs/config-fs/vmnet startup and failure propagation to QEMU/state.
- Continued iteration 3 added a state-writing supervised async QEMU runner surface. Next work should combine it with supervised service readiness into an async launch-runner skeleton that can start/observe blocking services and propagate service failure to QEMU/state.
- Continued iteration 2 added async path/service readiness primitives with supervisor status publication. Next work should combine the supervised QEMU and readiness helpers into an async launch-runner skeleton that can write launch state and fail QEMU when supervised services fail.
- Continued iteration 1 connected async QEMU process execution to `LaunchSupervisor` task status publication without changing production launch behavior. Next work should extend this pattern to an async launch-runner surface that writes state and supervises service readiness/failure ownership, while keeping composed-fs/config-fs request execution bounded blocking.
- Iteration 40 landed the first additive `wra-zqci` implementation slice: async QEMU/process spawn/wait/timeout helpers plus focused tests. Production `start_frontend_with_policy` is intentionally unchanged; next work should wire these helpers into a supervised async launch runner that owns service readiness/failure propagation and state updates before attempting ticket closure/required validation.
- Iteration 39 started `wra-zqci` and mapped the async supervision seams. Next priority is a small additive implementation slice, likely an async process/timeout/state helper or planned async launch-runner surface with tests, before replacing production `start_frontend_with_policy` wholesale.
- Iteration 38 closed `wra-jkeg` after required live-capable validation passed with no appliance rebuild request. Production launch-supervisor integration remains delegated to `wra-zqci`, and full TUI/control-socket semantics remain delegated to `wra-yl7i`; next priority is to inspect ready/dependency state and start the next option-1 ticket, likely async launch supervision (`wra-zqci`) before TUI control-socket work.
- Iteration 37 added the minimal async payload/TUI compatibility seam (`AsyncPayloadCommandSender`) and broader vm-frontend offline validation is green. `wra-jkeg` remains open because production launch/TUI still use the synchronous payload path; next priority is deciding whether to integrate async sessions into production now or explicitly leave production wiring to `wra-zqci`/`wra-yl7i` while keeping this ticket focused on reusable async client plumbing.
- Iteration 36 reflection: the overall approach remains sound, but `wra-jkeg` should avoid absorbing `wra-yl7i`'s full TUI control-socket scope. Added async cancellation, ping, and diagnostic deadline/output helpers with focused tests. Next priority: decide the minimal TUI bridge compatibility seam for async payload events, then run broader validation and assess whether `wra-jkeg` is ready for required validation/closure or needs integration into launch paths.
- Iteration 35 started `wra-jkeg` and landed the first additive async payload session plumbing: new `AsyncPayloadSession` with split Tokio stream ownership, bounded command/event channels, shared protocol codecs, and async tests for control frames/events/partial-frame errors. Existing synchronous launch/TUI paths are unchanged. Next priority: add async ping/diagnostic timeout/cancellation helpers and decide the minimal TUI bridge compatibility seam before broader validation.
- Iteration 34 closed `wra-cvmy` after required live-capable validation passed with no appliance rebuild request. The shared payload protocol crate now covers frame constants/kinds/decoder/encoder, request/event/control payload schemas, sync fragmentation/oversize tests, fuzz coverage, and Tokio-ready async frame read/write APIs; `payload-protocol` is included in the required validation gate. Next priority: inspect ready/dependency state and continue to dependent payload protocol work, likely `wra-jkeg` before `wra-662v`.
- Iteration 33 moved payload request/event schemas and event/control-frame helpers into `agentvm-payload-protocol`, preserved frontend API via re-exports, and added Tokio-ready async frame read/write APIs with pre-allocation oversize checks. Focused validation is green. Next priority: run broader vm-frontend/fuzz validation and decide whether `wra-cvmy` is ready for required validation/closure or needs one more cleanup slice.
- Iteration 32 started `wra-cvmy` and landed the first shared payload protocol slice: new `agentvm-payload-protocol` workspace crate, guest-service re-export, frontend frame helpers using shared encode/decode-header logic, and a new fuzz target for arbitrary payload protocol frames. Focused tests/checks pass. Next priority: continue moving request/event structs and add async codec-ready APIs before broader validation.
- Iteration 31 closed `wra-n0fe` after required live-capable validation passed with no appliance rebuild request. The Tokio application edge and supervisor skeleton are intentionally narrow; production launch internals remain synchronous/detached until the dependent async supervision tickets convert them. Next priority: inspect ready/dependency state and continue protocol/shared work, likely `wra-cvmy` before deeper launch/runtime async conversions.
- Iteration 30 added a no-spawn `run_until_shutdown` supervisor API, accepted the skeleton scope for `wra-n0fe`, and broader vm-frontend offline validation is green. Next priority: run `./vm-frontend/validate.sh required`; if it passes with no appliance rebuild request, add final note and close `wra-n0fe`.
- Iteration 29 extended the supervisor skeleton with watch-backed per-task ownership/readiness/failure APIs and focused tests. Next priority: decide whether this is enough for `wra-n0fe` acceptance or add a tiny no-spawn run-plan API, then run broader vm-frontend offline validation before required validation.
- Iteration 28 started `wra-n0fe` and landed the first Tokio/supervisor skeleton: async binary edge, direct Tokio dependency, and typed `LaunchSupervisor` task/shutdown APIs with blocking filesystem service classification. Next priority: extend the skeleton toward supervised task ownership/readiness/failure APIs while preserving the existing launch path and keeping composed-fs/vmnet internals synchronous for now.
- Iteration 27 closed `wra-rjt4` after required live-capable validation passed with no appliance rebuild request. Next priority: inspect ready/dependency state and start `wra-n0fe` if unblocked, introducing the Tokio application edge/supervisor skeleton with the new tracing/error boundaries in place.
- Iteration 26 reflection: tracing coverage for `wra-rjt4` is broad enough for acceptance pending live-capable validation. Boundary-first tracing slices have preserved behavior, and broader vm-frontend offline validation is green. The minimal `AGENTVM_LOG` subscriber is a conscious tradeoff to avoid dependency churn; keep it unless required validation or review shows a need for richer diagnostics. Next priority: run `./vm-frontend/validate.sh required`; if it passes with no appliance rebuild request, add final note and close `wra-rjt4`.
- Iteration 25 added vmnet runtime/event-log tracing and an env-controlled minimal subscriber (`AGENTVM_LOG`) for visible diagnostics without default output changes. Next priority: run broader vm-frontend offline validation, then required validation if green; if it passes with no appliance rebuild request, add final note and close `wra-rjt4`.
- Iteration 24 extended tracing through command dispatch, launch CLI payload lifecycle, and docker proxy edges. Next priority: inspect vmnet runtime event-log/proxy seams and decide the minimal tracing slice there; also decide whether to add a binary subscriber/configuration or keep events subscriber-agnostic for this ticket.
- Iteration 23 started `wra-rjt4` and added the first structured tracing slice in launch supervision. Next priority: continue instrumentation at command/launch CLI payload lifecycle and docker/vmnet runtime edges, then run broader validation.
- Iteration 22 closed `wra-r070` after required live-capable validation passed with no appliance rebuild request. Typed-error coverage is accepted for this ticket despite a few narrow shared-helper/final-formatting string adapters. Next priority: inspect ready tickets and continue dependency order, likely `wra-rjt4` tracing before `wra-n0fe` supervisor work.
- Iteration 21 reflection: typed-error coverage is now broad enough that further conversion should be judged against value. Project lock/reset, local smoke upstream, and no-net validation are now typed; full vm-frontend offline tests pass. Next priority: run required live-capable validation for `wra-r070`; if it passes with no appliance rebuild request, add final note and close the ticket. If validation exposes appliance rebuild needs, notify the user exactly what to rebuild and switch to other useful epic work.
- Iteration 20 pushed typed launch errors through the major launch configuration/mount/env helpers and full vm-frontend offline tests still pass. Remaining stringly launch bits are smaller shared-helper or final formatting seams (`parse_clap_matches`, `absolute_cli_path`, project lock/reset, readiness/no-net helpers, local smoke upstream, qemu status text). Next priority: either convert project lock/reset/no-net/readiness helpers or decide remaining bridges are acceptable and run required validation for `wra-r070`.
- Iteration 19 introduced typed `LaunchCliError` at the launch dispatch boundary and full vm-frontend offline tests still pass. Remaining launch internals still use `String` via `LaunchCliError::Message`, so next priority should be converting deeper helpers (`frontend_config_from_args`, `runtime_mounts`, project lock/reset, env/auth) or deciding the remaining bridge is acceptable for `wra-r070` before required validation.
- Iteration 18 introduced typed `WrapperError` for wrapper dispatch and full vm-frontend offline tests still pass. This reduced one major bridge: wrapper config/TLS errors now propagate typed to `CliError`; however parser-specific wrapper helpers and launch/self-test still stringify. Next priority: start `LaunchCliError` or convert `self_test.rs`, then run required validation once remaining stringly seams are acceptably narrowed.
- Iteration 17 converted appliance freshness and TLS bootstrap to typed module errors, and appliance tests now assert structured stale-artifact variants. Next priority remains reducing the bridge problem: either introduce a typed `WrapperError` or start a typed `LaunchCliError` so config/appliance/TLS/payload errors can propagate without `String` conversions.
- Iteration 16 reflection: the overall approach remains sound, but the next slices should focus on reducing error bridges instead of adding many isolated module enums that immediately stringify. `CliError` now preserves typed payload/vmnet errors at the binary boundary and full vm-frontend offline tests pass. Next priority: migrate wrapper/launch-facing seams so `From<ConfigError|PayloadCliError|VmnetCliError> for String` bridges can be removed or at least confined.
- Iteration 15 converted `payload_cli.rs` and `vmnet_cli.rs` to typed module errors and added structured assertions for payload env parsing and vmnet missing-socket failures. Temporary `From<...> for String` bridges now exist for config/payload/vmnet while top-level command errors are still stringly. Next priority: introduce a top-level `CliError`/`CommandError` boundary or convert `tls_bootstrap.rs`/`appliance.rs`/`launch_cli.rs` so rich errors can propagate without bridge conversions.
- Iteration 14 started `wra-r070`, inventoried stringly error seams, and converted `config.rs` to a typed `ConfigError`/`ConfigResult` first slice. Existing config semantics/messages are preserved; a temporary `From<ConfigError> for String` bridge remains until caller modules migrate. Next priority: convert another cohesive CLI-adjacent seam such as `payload_cli.rs`/`vmnet_cli.rs` or introduce a top-level command error so config errors stop crossing module boundaries as strings.
- Iteration 13 closed `wra-09v9` after required live-capable validation passed. Next priority: inspect `tk ready`/dependencies and start the next option-1 ticket in order, likely `wra-r070` (typed domain errors) before tracing/supervisor work.
- Iteration 12 extracted `launch_cli.rs`; production `main.rs` now appears sufficiently thin for `wra-09v9` acceptance (top-level dispatch plus shared tiny helpers; test module remains in the file). Next priority is `./vm-frontend/validate.sh required`; if it passes, close `wra-09v9`.
- Iteration 11 extracted `wrapper.rs`; full vm-frontend offline tests still pass. Remaining `main.rs` is ~3.5k lines and now primarily contains top-level dispatch, launch run path, project lock/reset, frontend launch config parsing, runtime mounts/env/auth, local smoke upstream, policy construction, readiness wait, shared parse helpers, and tests. Next priority: extract `frontend_cli.rs`/`launch_config.rs` or decide the current split is sufficient, then run `./vm-frontend/validate.sh required` before closing `wra-09v9`.
- Iteration 10 extracted `self_test.rs` and full vm-frontend offline tests pass. Remaining `main.rs` is still ~4k lines and contains wrapper CLI/launch assembly, frontend launch config parsing, runtime mounts/env/auth, local smoke upstream, policy construction, readiness wait, reset, and tests. Next priority: decide whether one more extraction (`frontend_cli.rs` or `wrapper.rs`) is required before `wra-09v9` acceptance, then run `./vm-frontend/validate.sh required` before closing.
- Iteration 9 extracted `tls_bootstrap.rs`; no behavior changed. `main.rs` is still large but now has clearer seams. Next priority should either extract `self_test.rs` or run full vm-frontend offline tests and decide whether this is sufficient to close `wra-09v9` after required validation.
- Iteration 8 extracted `config.rs`; no config semantics changed, only module boundaries. Next priority should be extracting another large cohesive block (`self_test.rs` or launch/frontend CLI assembly) and then running broader vm-frontend tests before deciding whether `wra-09v9` is sufficiently complete for required validation.
- Iteration 7 extracted `vmnet_cli.rs`; no config semantics changed. Next priority remains a larger module: either `config.rs`/setup-tool schema (with config-json.md review if semantics touched) or `self_test.rs`.
- Reflection after iteration 6: approach remains sound, but avoid only shaving tiny helpers forever. Next priority should be a larger cohesive module (`config.rs`/setup-tool schema, `frontend_cli.rs`, or `self_test.rs`) while keeping focused tests green. If moving config semantics, update `vm-frontend/config-json.md`; otherwise document no semantic change.
- Iteration 6 extracted `payload_cli.rs`; no config semantics or CLI behavior changed. Existing binary tests still cover guest sync and payload-client behavior via test-only imports.
- Iteration 5 completed first mechanical extraction for `wra-09v9`: appliance freshness is now isolated. No config semantics or CLI behavior changed. Next slice should be larger and still cohesive, likely config/setup-tool schema helpers or payload-client CLI parsing.
- Iteration 4 selected and started `wra-09v9`. First safe extraction should be mechanical and behavior-preserving; likely start with config/setup-tool schema and JSON helpers or a similarly cohesive block, with focused tests moved alongside as needed.
- Iteration 3 closed `wra-9glk` after live-capable required validation passed. Next unblocked option-1 ticket should be selected from dependents now that workspace boundaries exist, likely `wra-09v9` before `wra-r070`/`wra-rjt4`/`wra-n0fe`.
- Iteration 2 implemented `wra-9glk` workspace changes. Existing per-manifest validation commands did not require docs/script updates. Cargo now warns that `third_party/virtiofsd` has a member-local `[profile.release]` ignored under the root workspace; this is from the vendored/generated manifest and does not block metadata/tests.
- Iteration 1 selected `wra-9glk`: root has no `Cargo.toml`; manifests exist in `vm-frontend`, `guest-service`, `composed-fs`, `third_party/virtiofsd`, and `vm-frontend/fuzz`; Makefile currently uses per-manifest commands, so add the root workspace mechanically and preserve existing package invocations unless a script/doc update is genuinely needed.
- `wra-yl7i` owns TUI control-plane semantics; option 1 supervisor/payload tickets should provide clean backend boundaries rather than reimplement TUI coupling.
- Intended async boundary: Tokio at orchestration and byte-stream edges; synchronous deterministic smoltcp/protocol cores; synchronous composed-fs/vhost filesystem execution on bounded blocking workers.
